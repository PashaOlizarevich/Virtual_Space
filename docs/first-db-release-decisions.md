# Решения для первого DB-релиза

## 1. Назначение

Документ подробно раскрывает пункт 23 из `docs/implementation-plan.md` и объясняет решения, которые
должны оставаться неизменными при проектировании первой Prisma schema, миграций и серверных
контрактов Virtual Space.

Это единый подготовительный архитектурный документ для полных описаний решений из
`docs/implementation-plan.md`, относящихся к первому DB-релизу. Каждый выполненный пункт этого этапа
добавляется в данный файл отдельным блоком с обязательным заголовком `Пункт N — название`, после
которого идут назначение, принятые решения, обоснование, последствия и критерии соблюдения. Отдельный
документ на каждый пункт не создаётся.

Документ не означает, что PostgreSQL, Prisma-модели, миграции, Auth.js или production API уже
реализованы.

Под первым DB-релизом здесь понимается общий первый backend/data milestone, включающий foundation,
каталог, административную identity и заказы в последовательных миграциях, а не обязательно один
production deploy.

## Пункт 23 — Решения первого DB-релиза

### 23.1. Что было сделано

Для первого DB-релиза зафиксированы четыре границы:

1. заказ использует пять статусов: `NEW`, `CONFIRMED`, `IN_PROGRESS`, `COMPLETED`, `CANCELLED`;
2. товар конфигурируется через группы и значения опций без отдельных SKU-вариантов;
3. деньги хранятся точно как PostgreSQL `numeric` и Prisma `Decimal`, а наружу передаются
   decimal-строкой с валютой `BYN`;
4. Auth.js сначала защищает административный сценарий, а пользовательские аккаунты и серверная
   корзина относятся к следующему этапу.

Эти решения синхронизированы с `docs/architecture.md`. Последующие пункты плана должны реализовывать
их, а не повторно выбирать доменную модель.

### 23.2. Статусы заказа

#### Принятый контракт

Prisma enum и серверный доменный тип должны содержать только:

```text
NEW
CONFIRMED
IN_PROGRESS
COMPLETED
CANCELLED
```

Основной последовательный поток:

```text
NEW -> CONFIRMED -> IN_PROGRESS -> COMPLETED
```

Отмена разрешена из `NEW`, `CONFIRMED` и `IN_PROGRESS`. `COMPLETED` и `CANCELLED` являются
терминальными состояниями. Любой другой переход отклоняется серверным service-слоем.

Каждое изменение должно атомарно обновлять `Order.status` и добавлять `OrderStatusHistory` с
предыдущим и новым статусом, временем и optional идентификатором администратора.

#### Зачем

- `CONFIRMED` отделяет принятую заявку от новой и ещё не проверенной.
- `IN_PROGRESS` описывает реальное исполнение заказа без неоднозначного общего `PROCESSING`.
- ограниченный граф переходов не позволяет вернуть завершённый или отменённый заказ в работу
  случайным запросом;
- история даёт аудит административных действий и не зависит от текущего значения заказа.

UI, middleware и знание URL не являются механизмом защиты. Сервер обязан проверить Auth.js session,
роль `ADMIN`, входной enum и допустимость перехода.

### 23.3. Товарные опции без SKU-вариантов

#### Принятый контракт

Первый релиз использует:

- `ProductOptionGroup` — стабильный идентификатор/ключ, подпись и порядок группы;
- `ProductOption` — стабильный идентификатор/ключ, подпись и порядок значения внутри группы.

Например, группа «Материал» может содержать значения «Дуб» и «Орех». Выбранные стабильные
идентификаторы передаются в корзину и сохраняются снимком в позиции заказа.

`ProductVariant` не создаётся. Комбинация и отдельное значение опции не имеют собственного SKU,
price modifier, отдельной цены или остатка. Цена и доступность определяются товаром, а сервер
повторно проверяет выбранные значения при создании заказа.

#### Зачем

- текущий каталог не требует складского учёта каждой комбинации;
- преждевременные варианты усложнили бы уникальные ограничения, остатки, корзину и миграции;
- группы и значения сохраняют нужную пользователю конфигурацию без фиктивных складских сущностей;
- стабильные идентификаторы позволяют безопасно заменить mock-данные постоянным хранилищем.

`ProductVariant` добавляется отдельной совместимой миграцией только при появлении реального правила:
комбинация получает собственный SKU, цену или остаток. До этого отсутствие вариантов является
осознанным ограничением, а не незавершённой моделью.

### 23.4. Деньги и публичный DTO

#### Хранение

Денежные поля PostgreSQL используют `numeric` с явно выбранными precision и scale. Их значения,
правило округления и допустимая грамматика канонической строки определяются в пункте 35 до создания
использующей их миграции. Prisma представляет деньги как `Decimal`. `float`, `double precision` и
JavaScript `number` не являются источником истины для цен и итогов.

Валюта первого релиза — `BYN` и хранится явно там, где значение должно оставаться самостоятельным
денежным снимком, включая заказ.

#### Граница сервера

Prisma `Decimal` не передаётся в Client Components или JSON напрямую. Mapper формирует безопасный
сериализуемый DTO:

```ts
type MoneyDto = {
  amount: string;
  currency: "BYN";
};
```

`amount` — каноническая decimal-строка без форматированных пробелов, символа валюты или локальной
запятой, например `"1299.90"`. Отображение в локали выполняет formatter, но вычисление total,
проверка цены и округление остаются на сервере.

#### Зачем

- двоичная арифметика JavaScript не гарантирует точное представление десятичных денежных значений;
- строка безопасно проходит через JSON и не раскрывает внутренний Prisma-тип;
- явная валюта исключает неявное предположение в API и снимках заказа;
- единый mapper позволяет постепенно заменить текущие frontend-цены типа `number`.

Клиентские цена, скидка и итог считаются недоверенными. При оформлении сервер перечитывает товары,
проверяет доступность и пересчитывает сумму по данным PostgreSQL.

#### Рассмотренные альтернативы

- PostgreSQL `float` и JavaScript `number` отклонены из-за ошибок двоичной арифметики.
- Хранение только minor units не выбрано: нормативным контрактом проекта остаётся decimal, а
  правила точности будут единообразно заданы в пункте 35.
- Передача Prisma `Decimal` напрямую отклонена, потому что публичный контракт не должен зависеть от
  ORM и её сериализации.

### 23.5. Первый этап Auth.js

#### Объём первого релиза

Auth.js сначала реализуется для администратора и защищает:

- административные маршруты;
- Server Actions и Route Handlers изменения товаров, заказов и настроек;
- смену статуса и другие привилегированные service entry points.

Каждая защищённая серверная операция самостоятельно проверяет session и роль `ADMIN`. Ранний
redirect в middleware и скрытие элементов UI могут улучшать интерфейс, но не заменяют авторизацию.

#### Что отложено

На следующий этап перенесены:

- публичная регистрация и пользовательский вход;
- профиль, собственные заказы и восстановление доступа;
- постоянные `Cart`/`CartItem` и объединение гостевой корзины с серверной.

До этого гостевая корзина остаётся версионированным клиентским состоянием и не считается доверенным
источником цены, наличия или прав. Отложенные пользовательские сценарии не должны блокировать
защищённую админку и гостевое оформление заявки.

#### Зачем

- административная поверхность уже требует настоящей серверной границы доступа;
- пользовательская авторизация добавляет отдельные требования к lifecycle аккаунта, восстановлению,
  ownership и синхронизации корзин;
- поэтапное внедрение уменьшает первый DB-релиз и не вынуждает принимать политику пользовательских
  данных раньше соответствующих пунктов плана.

Одновременное внедрение административной и покупательской авторизации отклонено как преждевременное
расширение scope. Постоянная гостевая корзина на сервере также не вводится до появления
пользовательской identity и правил объединения корзин.

### 23.6. Последствия для последующих пунктов

| Область               | Обязательное продолжение                                                                     |
| --------------------- | -------------------------------------------------------------------------------------------- |
| Prisma foundation     | Настроить PostgreSQL и единый server-only PrismaClient без изменения решений этого документа |
| Каталог               | Создать `ProductOptionGroup` и `ProductOption`, не создавая `ProductVariant`                 |
| Денежный контракт     | Ввести общий `MoneyDto`, mapper из `Decimal` и formatter для UI                              |
| Заказы                | Создать enum из пяти статусов и атомарную историю допустимых переходов                       |
| Auth.js               | Сначала подключить администратора и server-side проверку роли                                |
| Пользовательский этап | Позже добавить аккаунты, ownership заказов, серверную корзину и безопасное объединение       |

Изменение любого из этих решений до реализации требует сначала обновить `docs/architecture.md`, этот
документ и связанные пункты плана, затем оценить совместимость миграции и frontend-контрактов.

### 23.7. Что не входит в пункт 23

- выбор PostgreSQL-платформы, pooling и release-процесс миграций — пункт 24;
- retention заказов, анонимизация пользователей и bootstrap первого администратора — пункт 25;
- Prisma config, schema и DB client — пункт 26;
- фактические модели, миграции, seed, services, DTO mapper и Auth.js-конфигурация — последующие
  пункты implementation plan;
- отдельные SKU-варианты, пользовательская авторизация и постоянная корзина — следующий этап после
  возникновения соответствующих требований.

### 23.8. Критерии соблюдения решения

При реализации последующих пунктов необходимо подтвердить:

- enum и server validation принимают ровно пять утверждённых статусов;
- недопустимые переходы отклоняются сервером и не создают частичную историю;
- option groups и options имеют стабильные идентификаторы, а `ProductVariant` отсутствует;
- schema использует `numeric`/`Decimal`, публичный DTO — decimal-строку и `BYN`;
- цена и total пересчитываются сервером, а Prisma-модели не выдаются наружу;
- административные операции проверяют действительную Auth.js session и роль `ADMIN` на сервере;
- пользовательские аккаунты и server cart не попадают в первый этап скрытым расширением scope;
- SQL каждой миграции просмотрен на потерю данных, блокировки и несовместимость развёртывания.

## Пункт 24 — PostgreSQL и развёртывание первого DB-релиза

### 24.1. Назначение

Раздел раскрывает пункт 24 из `docs/implementation-plan.md` и фиксирует платформу PostgreSQL,
изоляцию local/preview/production, connection pooling, доступ к секретам и release-процесс миграций.

Это подготовительное архитектурное решение. Оно не создаёт Neon/Vercel-ресурсы, не подключает
production-базу и не означает, что Prisma schema или миграции уже реализованы.

### 24.2. Принятое решение

- Next.js-приложение развёртывается на **Vercel**.
- Управляемый PostgreSQL размещается в **Neon** через Neon-managed Vercel Integration.
- Регион Neon выбирается максимально близко к региону Vercel Function, обслуживающему запросы к БД;
  конкретный регион фиксируется при создании ресурсов после проверки доступности для выбранного
  аккаунта и требований к размещению данных.
- Production использует долгоживущую защищённую Neon-ветку. Каждый preview deployment получает
  отдельную временную Neon-ветку. Local development использует отдельную dev-ветку, а не production.
- Приложение подключается через pooled URL; Prisma CLI, миграции, backup/restore и другое
  session-dependent администрирование используют direct URL.

Vercel Postgres как отдельный продукт не выбирается: новые PostgreSQL-базы подключаются через
Marketplace, а прежний Vercel Postgres был перенесён на Neon. Self-hosted PostgreSQL, Supabase,
Railway и Render не дают текущему Vercel-first проекту преимущества, оправдывающего дополнительную
операционную поверхность. Выбор провайдера можно пересмотреть до хранения production-данных, если
изменятся требования к региону, бюджету, SLA или юридическому размещению данных.

### 24.3. Матрица окружений

| Среда      | Приложение                   | PostgreSQL                                                    | Данные и жизненный цикл                                                                                  |
| ---------- | ---------------------------- | ------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| Local      | `npm run dev` на Node.js 24+ | отдельная Neon-ветка `dev/<developer-or-feature>`             | только тестовые/seed-данные; production credentials локально не выдаются                                 |
| Preview    | Vercel Preview на PR/ветку   | отдельная ephemeral Neon-ветка `preview/pr-<number>-<branch>` | создаётся до проверки миграции, получает собственные URLs, удаляется после закрытия PR по TTL/интеграции |
| Production | Vercel Production из `main`  | защищённая Neon-ветка `production`                            | реальные данные; запрет reset, auto-seed и использования из preview/local                                |

Preview не должен автоматически получать необработанную копию production-данных. До появления
процесса маскирования preview создаётся от безопасной непроизводственной baseline-ветки и заполняется
повторяемым seed. Если для диагностики позже потребуются реалистичные данные, источником может быть
только предварительно анонимизированная ветка с отдельно утверждёнными правилами доступа и retention.

### 24.4. Соединения и pooling

Neon выдаёт два server-only секрета:

```dotenv
DATABASE_URL=             # pooled endpoint, hostname содержит -pooler
DATABASE_URL_UNPOOLED=    # direct endpoint для release/admin tooling
```

Правила использования:

1. `DATABASE_URL` использует PrismaClient через `@prisma/adapter-pg` для всех runtime-запросов Vercel
   и local/preview приложения. В каждой среде URL указывает только на её Neon-ветку.
2. `DATABASE_URL_UNPOOLED` используется `prisma.config.ts` для `prisma migrate deploy`, разработки
   миграций, introspection, backup/restore и Prisma Studio. Runtime-код его не читает.
3. PrismaClient остаётся единым server-only singleton на процесс/warm instance. Нельзя создавать
   новый client на каждый запрос или подключать direct URL к serverless runtime.
4. Neon PgBouncer работает в transaction mode. Код не полагается на сохранение session-level
   `SET`, временных таблиц, `LISTEN/NOTIFY`, session advisory locks или cursors между транзакциями.
5. Транзакции должны быть короткими и не содержать сетевых вызовов. Исчерпание пула, время ожидания,
   число соединений и медленные запросы контролируются метриками Neon; размер compute/pool меняется
   по измеренной нагрузке, а не предварительно.
6. Оба URL обязаны требовать TLS согласно строкам подключения Neon. Параметры URL не собираются из
   отдельных фрагментов в коде и не логируются.

Хотя актуальные Prisma и Neon допускают часть CLI-операций через pooler, для release-процесса выбран
direct endpoint: это отделяет ограниченный административный доступ от массового serverless-трафика и
не зависит от session-ограничений transaction pooling.

### 24.5. Секреты и права доступа

- Значения хранятся только в Neon/Vercel secret storage или в локальном gitignored `.env.local`;
  репозиторий содержит лишь пустые имена в `.env.example`.
- `DATABASE_URL` доступен runtime-окружению соответствующего Vercel deployment. Он не имеет
  `NEXT_PUBLIC_`-префикса и никогда не передаётся в Client Components, ответы или логи.
- `DATABASE_URL_UNPOOLED` доступен только локальному доверенному оператору и отдельному migration job.
  Обычным Preview/Production Functions direct credential не требуется.
- Production и non-production используют разные credentials. Preview credential ограничен своей
  ephemeral-веткой; закрытие PR отзывает доступ удалением ветки/endpoint.
- Доступ людей выдаётся по принципу минимальных привилегий через Neon/Vercel accounts, с MFA для
  владельцев production и аудитом участников. Строки подключения ротируются после подозрения на
  раскрытие или изменения состава привилегированной команды.
- CI не печатает env, строки команд с URL, Prisma debug output с credentials и полные ошибки
  подключения. Проверка env сообщает только имя отсутствующей переменной.

### 24.6. Release-процесс миграций

Миграция является отдельным одноразовым release job и не запускается во время `next build`, старта
приложения, Server Action, Route Handler или serverless cold start.

#### Pull request / preview

1. Создать из безопасной baseline отдельную Neon preview-ветку и внедрить в deployment её pooled и
   direct URLs.
2. Установить зависимости через `npm ci`; выполнить `prisma validate` и `prisma generate`.
3. Прочитать SQL новой миграции и проверить `DROP`, сужение типа, table rewrite, `NOT NULL` без
   backfill, длительные блокировки, повторный запуск и совместимость старого/нового приложения.
4. Одноразово выполнить `prisma migrate deploy` через `DATABASE_URL_UNPOOLED` только на preview-ветке.
5. Выполнить тесты и сборку приложения против preview; seed запускать только явной отдельной командой.

#### Production

1. До релиза подтвердить backup/restore или точку восстановления и успешную проверку той же
   неизменённой миграции на preview/непроизводственной копии.
2. Остановить rollout при failed checks, drift, unresolved failed migration или отсутствии direct
   production credential у migration job.
3. Выполнить ровно один `prisma migrate deploy` с immutable commit/artifact через
   `DATABASE_URL_UNPOOLED`; job не принимает запросы пользователей и не запускает seed.
4. После успеха развернуть совместимую версию приложения и выполнить smoke-проверку. При ошибке
   приложения откатить код только если схема обратно совместима; миграцию не откатывать вручную.
5. Исправлять ошибку forward-fix миграцией. Для несовместимых изменений использовать отдельные
   релизы `expand -> migrate/backfill -> contract`; destructive contract требует отдельного
   подтверждения после переключения всех потребителей.

Одновременные production migration jobs запрещены. CI/CD должен сериализовать их через один
environment lock/concurrency group. `prisma migrate dev`, `prisma db push`, reset и автоматический
seed в production запрещены.

### 24.7. Критерии соблюдения

- local, каждый preview и production имеют изолированные Neon-ветки и credentials;
- runtime читает pooled `DATABASE_URL`, а release job — direct `DATABASE_URL_UNPOOLED`;
- preview/local не имеют production credentials и не содержат немаскированные production-данные;
- миграция проверена на preview и выполняется один раз до production rollout;
- cold start, build и обычный runtime никогда не применяют миграции;
- backup/restore, serialisation migration jobs и forward-fix определены до первого production deploy;
- секреты не попадают в Git, клиентский bundle, логи и deployment artifacts.

### 24.8. Что не входит в пункт 24

- создание аккаунтов, Neon/Vercel project, веток и credentials;
- добавление `prisma.config.ts`, schema, PrismaClient и миграций — пункт 26 и последующие пункты;
- добавление CI workflow или Vercel deployment configuration — пункт 82;
- выбор retention/backup-плана и юридического региона без фактических production-требований;
- миграция, seed, `db push`, production-запросы или изменение внешней инфраструктуры.

### 24.9. Официальные источники

- [Vercel: Postgres on Vercel](https://vercel.com/docs/postgres)
- [Vercel: deployment environments](https://vercel.com/docs/deployments/environments)
- [Vercel: environment variables](https://vercel.com/docs/environment-variables)
- [Neon: connection pooling](https://neon.com/docs/connect/connection-pooling)
- [Neon: database branching workflow](https://neon.com/docs/get-started-with-neon/workflow-primer)
- [Neon: Vercel integration variables](https://neon.com/docs/changelog/2024-02-23)
- [Prisma: Prisma Config reference](https://www.prisma.io/docs/orm/reference/prisma-config-reference)

## Пункт 25 — жизненный цикл заказов, аккаунтов и первого администратора

### 25.1. Назначение и границы

Раздел раскрывает пункт 25 из `docs/implementation-plan.md`. Он задаёт инварианты для будущих
Prisma-моделей, `onDelete`, сервисов удаления аккаунта и команды bootstrap, но не создаёт schema,
миграции, Auth.js-конфигурацию, production-данные или администратора.

Политика исходит из текущего размещения магазина в Беларуси. До начала production-продаж владелец
магазина обязан подтвердить применимый перечень документов и сроки с бухгалтером или юристом. Если
закон, договор, спор, возврат, налоговая проверка или legal hold требуют более долгого хранения, они
имеют приоритет над автоматическим удалением.

### 25.2. Хранение заказов

- `Order`, `OrderItem` и `OrderStatusHistory` являются самостоятельной историей хозяйственной
  операции и не удаляются вместе с аккаунтом покупателя, администратора или товаром.
- Для первого production-релиза применяется консервативный срок: **10 лет после окончания
  календарного года, в котором заказ завершён или отменён**. До юридического подтверждения нельзя
  настраивать меньший срок. Незавершённый заказ отсчитывает срок только после перехода в терминальный
  статус `COMPLETED` или `CANCELLED`.
- Заказ хранит неизменяемые снимки товарных данных, цены, валюты и выбранных опций. Контактный снимок
  покупателя хранится только в объёме, необходимом для исполнения заказа, претензий и обязательного
  учёта; маркетинговое согласие и аналитический профиль в заказ не копируются.
- После срока хранения периодическая server-only job необратимо удаляет или анонимизирует прямые
  идентификаторы покупателя и свободный комментарий. Денежная сумма, валюта, позиции, статусы и
  обезличенные временные данные могут остаться для финансовой отчётности и агрегированной аналитики,
  если по ним нельзя разумно восстановить личность.
- Legal hold блокирует purge только для явно отмеченных заказов, содержит причину и срок пересмотра и
  доступен ограниченной административной роли. Hold нельзя использовать как бессрочный режим по
  умолчанию.
- Purge выполняется повторяемыми ограниченными пакетами, ведёт аудит количества и идентификаторов
  обработанных записей без записи удалённых персональных данных в логи. Backup наследует тот же
  срок: данные исчезают из активной БД сразу по политике, а из резервных копий — по их ограниченному
  циклу ротации и не восстанавливаются обратно в active storage без повторного purge.

### 25.3. Удаление и анонимизация пользовательского аккаунта

Удаление аккаунта является серверной доменной операцией, а не каскадным `DELETE User`:

1. Повторно проверить действующую сессию, владение аккаунтом и подтверждение чувствительной операции.
2. В одной транзакции отозвать активные сессии, удалить `Account`, `Session`,
   `VerificationToken`, recovery credentials и активную корзину с позициями.
3. Не удалять заказы. Установить их optional `userId` в `NULL`, сохранив минимальный контактный
   снимок заказа до окончания его отдельного retention-срока.
4. Обезличить `User`: заменить email случайным непубличным уникальным значением, удалить имя,
   телефон и прочие необязательные поля, уничтожить password hash, установить `deletedAt` и запретить
   вход. Исходные значения, обратимый mapping и зашифрованная копия не сохраняются.
5. Записать технический audit event без прежнего email, телефона, session token и содержимого заказа.

Простой soft delete с сохранением профиля отклонён: он прекращает вход, но не достигает минимизации
персональных данных. Каскадное удаление заказов также отклонено, поскольку разрушает историю
операций. Повторная регистрация с прежним email создаёт новую identity и не возвращает старые заказы
автоматически; связывание возможно только отдельным проверенным процессом, если он будет утверждён.

Запрос на удаление может быть частично ограничен обязательным хранением заказа. Пользователю нужно
сообщить, какие категории данных удалены немедленно, какие сохранены, на каком основании и до какого
события. До реализации пользовательских аккаунтов текст privacy notice и фактическая процедура
должны быть согласованы с этой политикой.

### 25.4. Первый администратор

Первый `ADMIN` создаётся только отдельной одноразовой server-only CLI-командой из immutable
release artifact на доверенной машине или в вручную запущенном защищённом job:

- команда подключается к нужной БД через direct `DATABASE_URL_UNPOOLED`; URL находится в secret
  storage и не передаётся аргументом;
- email вводится интерактивно или через защищённую переменную job, пароль — только скрытым
  интерактивным prompt либо masked secret input. Пароль запрещено передавать CLI-аргументом,
  записывать в shell history, `.env.example`, seed, fixture, логи или репозиторий;
- ввод валидируется, пароль хешируется актуальной password-hashing функцией с индивидуальной солью;
  в БД сохраняется только hash. Конкретная библиотека и параметры выбираются в пункте 43 после
  проверки актуальной версии Auth.js и среды выполнения;
- транзакция берёт фиксированный PostgreSQL transaction-level advisory lock для bootstrap, затем с
  уникальным email создаёт пользователя с ролью `ADMIN` только если администраторов ещё нет. При
  наличии хотя бы одного `ADMIN` команда завершается без изменений; lock исключает создание двух
  первых администраторов с разными email при параллельном запуске;
- роль никогда не принимается из публичной регистрации, profile API или client input. Следующих
  администраторов создаёт уже аутентифицированный `ADMIN` отдельной защищённой операцией;
- успешный bootstrap пишет минимальный audit event (`actor=SYSTEM`, новый user id, время, request/job
  id), но не email и не credential. Ошибки не выводят connection string, password или hash;
- после создания оператор проверяет вход, ротирует временный пароль, если он был выдан через job
  secret, и удаляет одноразовый secret из окружения. Повторный запуск остаётся безопасным no-op.

Production seed не создаёт пользователей и не читает admin credentials. Миграция также никогда не
создаёт администратора: schema rollout и privileged identity имеют разные права, журнал и жизненный
цикл. Break-glass восстановление доступа требует отдельной документированной ручной процедуры с MFA
у инфраструктурного аккаунта и не ослабляет проверку «администратор уже существует» в обычной
bootstrap-команде.

### 25.5. Последствия для схемы и сервисов

| Область              | Обязательное продолжение                                                                            |
| -------------------- | --------------------------------------------------------------------------------------------------- |
| `Order`              | Optional `userId`; снимок покупателя и `retentionUntil`; отсутствие cascade от `User`               |
| `OrderStatusHistory` | Optional actor admin; удаление admin не разрушает историю переходов                                 |
| `User`               | `deletedAt`, nullable/anonymizable profile, уникальная identity без обратимого mapping              |
| Auth.js              | Сессии и связанные credentials удаляются при anonymization; роль задаёт только сервер               |
| Cart                 | Каскадно удаляется с аккаунтом; заказ от корзины не зависит                                         |
| Retention job        | Server-only, пакетная, идемпотентная, учитывает legal hold и backup lifecycle                       |
| Bootstrap            | Отдельная CLI-команда; direct DB secret, скрытый password input, transaction lock, безопасный no-op |

Точные имена полей могут быть уточнены при проектировании schema, но перечисленные отношения,
границы удаления и запрет хранения bootstrap credentials являются обязательными.

### 25.6. Проверки реализации

- удаление пользователя отзывает вход, удаляет корзину и credentials, но сохраняет заказ и его
  позиции;
- после анонимизации ни один пользовательский endpoint, лог или audit event не раскрывает прежние
  email, телефон, имя или session data;
- FK `Order.userId` и actor истории допускают `NULL`/`SetNull`, а заказ не использует cascade delete;
- purge не трогает записи до `retentionUntil`, пропускает legal hold и безопасно повторяется;
- backup restore не возвращает просроченные персональные данные в активную эксплуатацию;
- bootstrap не принимает роль с клиента, не содержит фиксированного пароля и не печатает секреты;
- два параллельных запуска создают не более одного первого администратора;
- повторный запуск после успеха завершается без изменения существующих пользователей;
- seed, миграции, Git history и deployment logs не содержат admin credentials.

### 25.7. Что не входит в пункт 25

- юридическое заключение, утверждение публичной privacy policy и выбор окончательного архивного
  перечня;
- создание Prisma-полей, FK, миграций, retention job или Auth.js — последующие пункты 26–45 и 70;
- фактический запуск команды, создание production-администратора или чтение secret storage;
- MFA приложения, восстановление доступа и управление дополнительными администраторами;
- конкретный backup-retention тариф Neon, который утверждается до production provisioning.

### 25.8. Основания и обязательная верификация

- [МНС Республики Беларусь: изменения законодательства о бухгалтерском учёте](https://nalog.gov.by/news/14945/)
- [Национальный центр защиты персональных данных: официальный канал](https://t.me/s/cpd_by)

Источники подтверждают, что сроки учётных документов задаются специальным законодательством, а
право на удаление персональных данных не отменяет обязательное хранение документов. Они не заменяют
определение применимой категории документа для конкретного магазина; поэтому 10-летний срок в этом
решении является консервативной технической границей до профильного подтверждения, а не юридическим
заключением.

## Пункт 26 — базовая инфраструктура Prisma

Реализован недеструктивный schema-only фундамент без доменных таблиц и без миграции:

- `prisma.config.ts` указывает на `prisma/schema.prisma` и каталог `prisma/migrations`, а для CLI,
  introspection и будущих миграций требует direct-подключение `DATABASE_URL_UNPOOLED`;
- `prisma/schema.prisma` фиксирует PostgreSQL datasource и генератор `prisma-client-js`. Модели не
  добавлены преждевременно: их связи, ограничения и индексы относятся к следующим пунктам плана;
- `src/server/db.ts` помечен `server-only`, требует только pooled `DATABASE_URL`, создаёт
  `@prisma/adapter-pg` и переиспользует один `PrismaClient` через `globalThis`, включая hot reload;
- runtime-код не читает direct URL, Prisma config не читает pooled URL, значения секретов не
  хранятся и не выводятся;
- миграция, подключение к реальной БД, `db push`, seed и создание внешней инфраструктуры не
  выполнялись.

При запуске Prisma CLI оператор или release job должен заранее внедрить
`DATABASE_URL_UNPOOLED`. При запуске серверного data-access слоя Next.js должен внедрить
`DATABASE_URL`; отсутствие переменной приводит к fail-fast ошибке только с её безопасным именем.

## Пункт 27 — модели каталога и настроек магазина

В `prisma/schema.prisma` созданы семь самостоятельных валидных каркасов доменных моделей:

| Модель                 | Назначение в первом DB-релизе                                                       |
| ---------------------- | ----------------------------------------------------------------------------------- |
| `Category`             | Категория общего каталога, с которой позднее связываются товары                     |
| `Product`              | Основная запись товара и будущий источник истины для цены, остатка и публикации     |
| `ProductImage`         | Упорядоченное изображение галереи товара и будущая ссылка на Cloudinary             |
| `ProductSpecification` | Отдельная именованная характеристика товара                                         |
| `ProductOptionGroup`   | Группа выбираемых свойств товара без создания SKU-варианта                          |
| `ProductOption`        | Допустимое значение внутри группы опций                                             |
| `StoreSettings`        | Постоянное хранилище основной или именованной конфигурации магазина и его контактов |

Каждая модель на этом шаге содержит только обязательный для валидной Prisma-модели первичный ключ
`id BigInt @id @default(autoincrement())`. Последовательный PostgreSQL `bigint identity` выбран как
компактный локальный ключ для одной базы данных: он не требует расширения UUID, сохраняет хорошую
локальность B-tree и оставляет публичные `slug`, номера и DTO-идентификаторы независимыми от
внутреннего ключа. При передаче такого id через JSON server mapper обязан преобразовать Prisma
`BigInt` в строку; Prisma-модель напрямую публичным DTO не является.

Изменение классифицировано как недеструктивное `schema-only`: до пункта 27 доменных таблиц не было,
существующие поля и данные не удаляются и не преобразуются. SQL-миграция намеренно не создана,
`prisma db push`, подключение к PostgreSQL и seed не запускались. Фактические таблицы появятся только
в отдельной версионируемой миграции после завершения относящихся к ним требований схемы.

### Принятые границы пункта

- обязательные поля каталога, нормализованные уникальные `slug`, `Decimal`-цена, валюта `BYN`,
  остаток, публикация, период новинки, timestamps и внешние ключи относятся к пункту 28;
- `ProductVariant` не создавался: решение об отсутствии SKU-вариантов закрепляется пунктом 29;
- стабильные ключи групп и значений опций относятся к пункту 30;
- составные unique-ограничения и индексы под реальные пути запросов относятся к пункту 31;
- отношения и `onDelete` не добавлены преждевременно, поскольку их окончательный набор зависит от
  обязательных FK и ограничений следующих пунктов;
- frontend, mock/DTO-контракты, маршруты и пользовательские сценарии не изменялись.

Такое разделение сохраняет атомарность плана: пункт 27 фиксирует существование и названия сущностей,
но не выдаёт незавершённый набор полей и индексов за готовую модель каталога. Каркасы нельзя
использовать как production-контракт до выполнения пунктов 28–31 и создания проверенной миграции.

### Проверки и эксплуатационные последствия

- `prisma validate` подтвердил валидность схемы;
- `prisma generate` успешно создал Prisma Client 7.9.1 с новыми моделями;
- ESLint, TypeScript и production build Next.js прошли;
- `git diff --check` прошёл;
- Jest не запустился из-за существующей вне пункта 27 конфигурации: `jest.config.ts` ссылается на
  отсутствующий `next/dist/build/swc/jest-transformer.js`;
- новые переменные окружения и секреты не добавлялись; для CLI-проверок использовалось только
  безопасное локальное placeholder-значение ранее определённого `DATABASE_URL_UNPOOLED`, без
  подключения к серверу;
- `docs/ProductTour.md` не обновлялся, потому что пользовательские возможности не изменились.

## Пункт 28 — обязательные поля и связи каталога

Каркасы каталога расширены до предметной Prisma-схемы. `Category` и `Product` получили обязательные
нормализованные slug в формате lowercase kebab-case и глобальные unique-ограничения. Нормализация
выполняется до записи на серверной границе; база гарантирует уникальность уже канонического значения.
Публичный маршрут товара поэтому остаётся независимым от внутреннего `BigInt` id.

`Product` хранит обязательные `categoryId`, название, описание, материал, стиль и размеры, точную цену
`Decimal(12, 2)`, enum-валюту `BYN`, целочисленный остаток, состояние публикации `isActive`, nullable
`newFrom`/`newUntil` как PostgreSQL `timestamptz(3)`, а также `createdAt`/`updatedAt`. Остаток по умолчанию
равен нулю, новый товар по умолчанию не опубликован. Ограничения `price >= 0`, `stock >= 0` и
согласованности периода будут добавлены в SQL-миграции на предусмотренном планом этапе CHECK-ограничений;
до этого те же инварианты обязана проверять серверная команда записи.

Управление новинкой имеет один канонический контракт: выключенный переключатель записывает обе даты
как `NULL`; включённый требует начало и окончание, причём рекомендуемое окончание формы вычисляется как
начало плюс 30 календарных дней. Это UI/server default, а не DB default: PostgreSQL не должен зависеть
от момента вставки, если администратор выбрал другую дату. Публичный каталог считает новинку активной
только внутри включительного интервала `newFrom <= now <= newUntil`.

Дочерние записи получили обязательные FK и порядок: изображения содержат Cloudinary public id,
защищённый URL, alt и `position`; характеристики — label, value и `position`; группы и значения опций —
label и `position`. Категория защищена от удаления при существующих товарах (`Restrict`), а полностью
принадлежащие товару изображения, характеристики, группы и значения удаляются каскадно. Все сущности
каталога имеют timezone-aware timestamps. Стабильные ключи опций, составные unique-ограничения и
индексы остаются в пунктах 30–31, миграция — в пункте 32.

Изменение недеструктивное `schema-only`: таблицы ещё не создавались, SQL-миграция, подключение к БД,
`db push` и seed не выполнялись. `StoreSettings` не расширялся, поскольку пункт 28 ограничен каталогом.

## Пункт 29 — отсутствие товарных SKU-вариантов в первом релизе

Проверена и сохранена модель первого DB-релиза без `ProductVariant`. Цена, валюта, остаток и
доступность принадлежат `Product`; `ProductOptionGroup` и `ProductOption` описывают только допустимый
выбор покупателя. Ни отдельное значение, ни комбинация выбранных опций не имеют собственного SKU,
цены, модификатора цены или складского остатка.

Фактическая `prisma/schema.prisma` соответствует этому контракту: модель `ProductVariant`, ссылки на
неё и variant-таблицы отсутствуют, а каталог использует прямую связь
`Product -> ProductOptionGroup -> ProductOption`. Каталог миграций также не содержит SQL для
вариантов. Поэтому пункт не требует изменения схемы или создания пустой миграции.

`ProductVariant` можно добавить только отдельным последующим изменением, если появится подтверждённый
доменный инвариант: конкретная комбинация опций получает собственный SKU, цену либо остаток. Такое
изменение должно быть совместимым `expand`-шагом: новая таблица и nullable-связи создаются отдельной
версионируемой миграцией, существующие товары продолжают работать по товарной цене и остатку, а
переключение чтения и записи выполняется лишь после backfill и проверки данных. Пункт 29 не резервирует
схему такой таблицы и не предполагает скрытого variant id в текущих контрактах.

Классификация результата: подтверждение существующей schema boundary без изменения схемы и данных;
оно совместимо и недеструктивно. PostgreSQL не подключался, `prisma db push`, migration, seed и
backfill не выполнялись. Пункты 30–32 по стабильным ключам опций, ограничениям, индексам и первой
миграции остаются самостоятельными.

## Пункт 30 — стабильные идентификаторы товарных опций

В `ProductOptionGroup` и `ProductOption` добавлено обязательное строковое поле `key`. Внутренний
`BigInt id` остаётся первичным ключом и используется только для связей внутри PostgreSQL, а `key`
становится стабильным доменным идентификатором, который можно передавать в каталог и корзину и
сохранять в неизменяемом снимке позиции заказа.

Ключи задаются в каноническом lowercase kebab-case (`upholstery`, `leg-color`, `warm-beige`) и не
формируются из изменяемой пользовательской подписи автоматически. Область идентичности группы —
товар, область идентичности значения — группа: группа определяется парой `(productId, key)`, а
значение — парой `(groupId, key)`. Одинаковый ключ допустим у разных товаров или в разных группах.
После использования в корзине или заказе ключ нельзя переименовывать: новая смысловая опция получает
новый ключ, а отображаемый `label` можно менять независимо.

Публичные контракты называют эти значения `groupId` и `optionId` для совместимости с уже работающим
mock-каталогом, но server mapper должен заполнять их из `ProductOptionGroup.key` и
`ProductOption.key`, а не сериализовать внутренние `BigInt id`. Снимок заказа позднее сохраняет как
ключи, так и отображаемые подписи, чтобы история не зависела от изменений каталога.

Изменение классифицировано как совместимое недеструктивное `schema-only`: таблицы ещё не создавались,
данные и backfill отсутствуют, SQL-миграция, `db push` и seed не выполнялись. Составные
unique-ограничения для указанных областей идентичности намеренно относятся к пункту 31, а первая
версионированная миграция — к пункту 32.

## Пункт 31 — ограничения и индексы каталога и настроек

Схема закрепляет уникальность данных в тех же областях, в которых приложение их получает и
упорядочивает. Глобальные `Category.slug` и `Product.slug` остаются уникальными публичными
идентификаторами. Для изображений уникален Cloudinary public id, а позиция уникальна в пределах
товара. Для характеристик в пределах товара уникальны и подпись, и позиция. Для групп и значений
опций закреплены области стабильных ключей `(productId, key)` и `(groupId, key)`, а также уникальный
порядок `(productId, position)` и `(groupId, position)`. Эти составные unique-ограничения одновременно
дают PostgreSQL индексы с внешним ключом в первом столбце, поэтому отдельные дублирующие FK-индексы
для дочерних таблиц не создаются.

Публичный каталог получает активные товары в стабильном порядке `createdAt DESC, id DESC`; выборка
категории добавляет равенство по `categoryId`, поэтому для этих путей заданы отдельные составные
индексы. Выборка `/new` фильтрует опубликованные товары по включительному интервалу новинки и
сортирует по `newFrom DESC`; соответствующий индекс начинается с `isActive`, затем содержит
`newFrom`, `newUntil` и стабильный tie-breaker `id`. Категории сортируются по `name, id`. Индексы по
цене, материалу и стилю намеренно не добавлены: фактический server query и подтверждённая
селективность этих фильтров ещё отсутствуют, а преждевременные индексы увеличили бы стоимость записи.

`StoreSettings` получает стабильный уникальный строковый ключ с каноническим значением `primary` по
умолчанию. Серверный слой должен получать основную конфигурацию по этому ключу, а не предполагать
конкретное значение автоинкрементного `BigInt id`; это сохраняет возможность позднее добавить
именованные конфигурации без смены идентификатора основной записи.

Изменение классифицировано как совместимое недеструктивное `schema-only`: первая миграция каталога
ещё не создана, таблиц и данных для backfill нет. SQL-миграция, подключение к PostgreSQL, `db push` и
seed не выполнялись; создание и ручная проверка SQL остаются границей пункта 32.

## Пункт 32 — первая миграция каталога и настроек

Создана первая версионируемая Prisma-миграция
`20260830120000_create_catalog_and_settings`. Она получена офлайн как diff от пустой PostgreSQL-схемы
к текущей `prisma/schema.prisma` и создаёт enum `Currency`, таблицы каталога и `StoreSettings`,
первичные и внешние ключи, unique-ограничения и индексы пункта 31. `migration_lock.toml` фиксирует
провайдер `postgresql` для последующих миграций.

Изменение классифицировано как совместимое недеструктивное `schema-only`: это первая миграция, поэтому
она не меняет существующие таблицы каталога и не требует backfill. Ручная проверка SQL подтвердила
отсутствие `DROP`, `TRUNCATE`, удаления данных, сужения типов, `ALTER COLUMN` и добавления `NOT NULL`
к существующим таблицам. Обязательные поля создаются только вместе с новыми пустыми таблицами.

Миграция использует обычные `CREATE TABLE`, `CREATE TYPE`, `CREATE INDEX` и `ALTER TABLE ... ADD
CONSTRAINT`. При первом развёртывании они берут только краткие DDL-lock на создаваемые объекты; rewrite
или длительная блокировка существующих прикладных таблиц отсутствуют. Применение предполагает пустую
целевую схему без одноимённых объектов. Если в окружении уже есть созданные вне Prisma таблицы или enum,
сначала требуется отдельная сверка и baseline, а не принудительный запуск этой миграции.

SQL побайтно по содержимому после нормализации переводов строк сопоставлен с актуальным Prisma diff.
Реальная PostgreSQL не подключалась, миграция не применялась, `prisma db push`, reset, seed и
production-запросы не выполнялись. Применение остаётся отдельным release-шагом через
`prisma migrate deploy` после проверки целевого окружения.

## Пункт 33 — повторяемый seed каталога и настроек

Подготовлена явная Prisma seed-команда `tsx prisma/seed.ts`, зарегистрированная в
`prisma.config.ts`. Источником стартовых данных остаются текущие типизированные mock-контракты
каталога и профиля магазина; преобразование в DB-представление вынесено в `prisma/seed-data.ts` и
не меняет frontend-потребителей до интеграционного пункта 38.

Категории и товары адресуются по уникальному `slug`, основная конфигурация магазина — по ключу
`primary`, группы и значения опций — по составным стабильным ключам, изображения — по стабильному
`cloudinaryPublicId`, полученному из версионируемого mock-пути. Характеристики обновляются по
`(productId, label)`. Все известные записи создаются или обновляются через `upsert`; seed не содержит
`delete`, `deleteMany`, `TRUNCATE` или сверку с удалением отсутствующих записей, поэтому данные,
которых нет в стартовом наборе, сохраняются.

Seed выполняет каталог и настройки в одной интерактивной транзакции с увеличенным timeout. Для
первичного перехода локальные пути изображений записываются в существующее поле `secureUrl`, а
стабильный public id резервируется под последующую загрузку в Cloudinary. Отсутствующие в mock-модели
операционные поля получают явные стартовые значения: `stock = 10`, `isActive = true`, стиль
`Современный`; материал и размеры берутся из одноимённых характеристик с безопасным текстовым
fallback. Денежные значения передаются в Prisma decimal-строками с двумя знаками после запятой.

Чтобы настройки действительно сохраняли текущий `storeProfile`, модель `StoreSettings` расширена
полями `name`, `description`, `contacts`, `socials` и timestamps. Изменение классифицировано как
совместимое `schema + data`: миграция только добавляет столбцы, временные defaults обеспечивают
backfill возможной существующей записи, после чего прикладные defaults удаляются. `DROP` таблиц,
сужения типов и удаления строк нет; rollback выполняется forward-fix миграцией, поскольку удаление
заполненных столбцов было бы destructive.

Реальная PostgreSQL не подключалась, миграция и seed не применялись: запуск seed остаётся отдельным
явным действием оператора и запрещён для production. Проверка mapping покрывает полноту mock-каталога,
уникальность стабильных ключей, позиции дочерних записей и профиль `primary`.

## Связанные документы

## Пункт 34 — публичные DTO каталога и настроек

Для каталога и публичных настроек введены отдельные строгие Zod-схемы в server-областях доменных
модулей. Контракты являются явным allowlist и не выводятся из Prisma-моделей: неизвестные поля,
Prisma `Decimal`, даты не в ISO 8601 и несериализуемые значения отклоняются на границе.

Каталожный DTO сохраняет только необходимые UI публичные идентификаторы, slug, контент, безопасное
представление цены, изображения, характеристики и группы опций. Cloudinary public id, числовые DB id
связей, остаток, `isActive` и timestamps остаются внутренними. DTO настроек содержит только название,
описание, контакты и HTTPS/social либо разрешённые `tel:`/`mailto:` ссылки; `StoreSettings.id`, `key` и
timestamps наружу не передаются. Размеры строк и коллекций ограничены для защиты будущих публичных
transport-границ от чрезмерных payload.

На этом пункте не добавлены Prisma-запросы и mapper: выборки только необходимых полей относятся к
пункту 36, а преобразование результатов Prisma — к пункту 37. Общий денежный mapper и frontend
formatter остаются объёмом пункта 35.

## Пункт 35 — единый денежный контракт

В `src/shared/money.ts` определён единый `MoneyDto` с формой `{ amount: string; currency: "BYN" }`.
Канонический `amount` неотрицателен, не содержит ведущих нулей, всегда имеет два знака после точки и
ограничен десятью цифрами целой части в соответствии с `Decimal(12, 2)`. Общая Zod-схема переиспользуется
каталожным DTO вместо отдельного ослабленного регулярного выражения.

Mapper принимает Prisma-compatible Decimal-like значение через его точный `toFixed(2)` и возвращает
только прошедший общую схему JSON-safe объект. Временный вход `number` оставлен исключительно для
поэтапного перевода существующих mock/frontend-моделей; он не является серверным источником истины.
Обратное преобразование в `number` применяется только для отображения, а не для расчёта итогов.

Общий formatter локализует и `MoneyDto`, и legacy `number` в `ru-BY`/`BYN`, показывая до двух дробных
знаков. Существующие каталог, корзина, профиль и административные mock-интерфейсы переведены с локальных
экземпляров `Intl.NumberFormat` на этот formatter. Prisma-запросы и доменные mapper результатов остаются
объёмом пунктов 36–37; серверное вычисление заказа — последующих пунктов заказов.

- `docs/implementation-plan.md` — пункт 23 и последующие шаги реализации;
- `docs/architecture.md` — нормативные архитектурные границы, данные, потоки и безопасность;
- `docs/TermsOfReference.md` — продуктовые требования к заказам и административному управлению;
- `docs/ProductTour.md` — фактически реализованные пользовательские и административные поверхности;
- `docs/progress.md` — история завершённых изменений и результаты проверок.

## Пункт 36 — server-only чтение каталога и публичных настроек

Для `catalog` и `settings` добавлен отдельный server-only DAL. Prisma-запросы используют явные
`select`: категории читают только `slug` и название; список товаров — публичный контент, цену,
период новинки и первое изображение; карточка товара дополнительно читает упорядоченные галерею,
характеристики и группы опций; настройки магазина — только название, описание, контакты и соцсети.

Публичный каталог возвращает только `isActive = true`, поддерживает необязательный фильтр категории
и keyset-пагинацию в стабильном порядке `createdAt DESC, id DESC`. Service ограничивает страницу
100 товарами, валидирует category/product slug и получает одну дополнительную строку для определения
следующей страницы. Товар ищется одновременно по уникальному slug и признаку активности; основной
профиль магазина адресуется ключом `primary`.

Изменение является query-only: Prisma schema, миграции и данные не изменялись, реальная БД не
подключалась. Результаты пока остаются внутренними Prisma-shaped records; их преобразование в
безопасные JSON DTO является отдельным пунктом 37.

## Пункт 37 — mapper результатов Prisma в публичные DTO

В server-областях `catalog` и `settings` добавлены отдельные доменные mapper-функции. Service больше
не возвращает Prisma-shaped records: категории, превью и детали товара, публичные настройки и cursor
пагинации преобразуются в соответствующие строгие DTO до выхода из server-only слоя.

Внутренний `BigInt` товара и cursor преобразуется в decimal-строку, даты — в ISO 8601 UTC, Prisma
`Decimal` — в общий `MoneyDto`. Группы и значения опций получают публичные идентификаторы из
стабильных `key`, а не DB id. Cloudinary public id, `createdAt`, остаток, флаг активности и другие
служебные поля не попадают в результат. JSON-колонки контактов и соцсетей настроек считаются
недоверенными и повторно проверяются через строгую Zod-схему с безопасными URL.

Mapper намеренно отклоняет товар без основного изображения и любую запись, нарушающую публичный
контракт, вместо выдачи частично валидного DTO. Изменение является query/contract-only: Prisma schema,
миграции и данные не менялись, реальная PostgreSQL не подключалась. UI и transport остаются объёмом
следующих пунктов плана.

## Пункт 38 — подключение публичных страниц к PostgreSQL

Главная, общий каталог, десять явных страниц категорий, `/product/<slug>`, `/about` и общий футер
переведены на server-only services каталога и настроек. Страницы получают только allowlisted JSON-safe
DTO; Prisma и внутренние поля не пересекают серверную границу. Независимые выборки главной выполняются
параллельно, продукт дедуплицируется между metadata и page через request-local React cache.

Публичный store layout рендерится динамически, чтобы production build не требовал доступной БД и
каждый запрос видел актуальные данные. Клиентский mock query каталога удалён; сами исходные mock-данные
сохранены только там, где они ещё нужны seed, новинкам, акциям, избранному и временной сверке корзины.
UI принимает `MoneyDto`, преобразуя сумму в `number` лишь для недоверенного гостевого состояния корзины;
серверный пересчёт заказа остаётся обязательным последующим этапом.

Изменение не меняет Prisma schema, миграции или данные и не подключает preview-админку к записи
`StoreSettings`. Если основная запись настроек отсутствует либо БД недоступна, публичный render
завершается контролируемой серверной ошибкой, а не молча возвращается к mock-данным.

## Пункт 39 — проверка каталога и БД

Контрольный набор подтвердил инварианты первого публичного DB-среза: уникальность `Category.slug` и
`Product.slug` закреплена Prisma schema и SQL-миграцией, а seed дополнительно проверяет уникальность
slug исходного каталога и стабильных дочерних ключей. Оба публичных товарных запроса применяют
`isActive = true`; каталог и карточка не могут вернуть деактивированный товар через текущий DAL.

Денежный mapper преобразует Prisma-compatible Decimal через точный `toFixed(2)` в канонический
`MoneyDto`, а строгие DTO и mapper'ы отклоняют ORM-значения, внутренние поля, небезопасные URL,
неканонические суммы и товар без основного изображения. Запросы каталога и настроек используют явные
`select`, стабильную пагинацию и профиль настроек `primary`. Целевые 8 suites / 28 тестов прошли.

Read-only security-review блока 26–39 не выявил подтверждённых findings: Prisma импортируется только
в server-only слой, публичные ответы формируются allowlist DTO, JSON-поля настроек повторно
валидируются, raw SQL и клиентское доверие цене отсутствуют. Реальная PostgreSQL не подключалась,
миграция и seed не применялись. Полный Jest остаётся красным из-за 12 существующих page-тестов после
пункта 38; их изменение требует отдельного разрешения согласно правилам проекта.

## Пункт 40 — модели Auth.js

В Prisma schema добавлены совместимые с официальным Prisma Adapter модели `User`, `Account`, `Session`
и `VerificationToken`. Auth identity использует отдельный строковый `cuid`, поскольку контракт adapter
оперирует строковым `userId`; доменные `BigInt`-идентификаторы каталога от этого не меняются.

Выбрана database session strategy: `Session` хранит уникальный случайный `sessionToken`, срок действия
как `timestamptz(3)` и обязательную ссылку на пользователя. `Account` содержит полный adapter-контракт
OAuth/OIDC и удаляется каскадно вместе с `User`; то же правило применяется к активным сессиям.
`VerificationToken` не связан FK с пользователем, потому что до подтверждения identifier может не
соответствовать существующей записи. Token уникален, а пара `identifier + token` является primary key.

Изменение классифицировано как совместимое `schema-only`: существующие модели и данные не меняются,
SQL-миграция и дополнительные поисковые индексы намеренно оставлены пункту 42. Обязательный уникальный
`email` добавлен как часть adapter-контракта; его серверная нормализация, а также поля `role`, `phone`,
`passwordHash` и lifecycle-поля пользователя относятся к пункту 41 и не добавлены преждевременно.
Prisma Adapter ещё не установлен и не подключён — это часть backend-интеграции следующих пунктов;
schema сверена с контрактом установленного Auth.js 5 и
официальной схемой adapter. Реальная PostgreSQL не подключалась, `db push`, migrate и seed не запускались.

## Пункт 41 — роли и credentials-поля пользователя

В Prisma schema добавлен enum `UserRole` со значениями `USER` и `ADMIN`; поле `User.role` получает
безопасное значение `USER` по умолчанию. Роль является server-managed атрибутом и не должна
приниматься из регистрации, формы профиля или другого клиентского payload.

В `User` добавлены nullable `phone` и `passwordHash`. Nullable hash поддерживает одновременно
Credentials identity и пользователей без локального пароля; открытый пароль в схеме отсутствует.
Телефон не является идентификатором входа и пока не получает unique-ограничение.

Единственным email-полем остаётся `User.email` с существующим unique-инвариантом. Его каноническое
значение формируется на каждой будущей server-side write-границе через `trim` и lowercase до записи
или поиска; отдельное дублирующее `normalizedEmail` не вводится. Prisma/PostgreSQL сами по себе не
приводят регистр, поэтому подключение Auth.js и Credentials в следующих пунктах обязано использовать
единую нормализацию и обрабатывать unique-конфликт.

Изменение классифицировано как совместимое `schema-only`: существующие строки при будущей миграции
получат `USER`, nullable-поля не требуют backfill, данные не удаляются и типы не сужаются. SQL-миграция
и индексы остаются пунктом 42; реальная PostgreSQL не подключалась, `db push` и migrate не запускались.

## Пункт 42 — миграция Auth.js и поисковые индексы

Создана версионируемая additive-миграция для enum `UserRole` и таблиц `User`, `Account`, `Session`,
`VerificationToken`. Миграция не изменяет существующие таблицы каталога и настроек, не требует
backfill и не содержит `DROP`, сужения типов или преобразования существующих строк.

Пути Auth.js закреплены индексами по фактическим lookup: unique `User.email`, составной primary key
`Account(provider, providerAccountId)`, unique `Session.sessionToken` и unique
`VerificationToken.token`. Для внешних ключей дополнительно добавлены `Account.userId` и
`Session.userId`, чтобы relation lookup и каскадное удаление пользователя не требовали полного
сканирования дочерних таблиц. Отдельные дублирующие индексы по `provider`, `identifier`, `expires` или
`role` не добавлены без связанного запроса.

Prisma schema успешно прошла validate/generate, а полный DDL, сгенерированный Prisma из schema,
подтвердил совпадение типов, ключей, индексов и FK с миграцией. Реальная PostgreSQL не была доступна,
поэтому миграция не применялась; перед общим или production deploy остаётся обязательным migration
job через direct `DATABASE_URL_UNPOOLED` с предварительным просмотром SQL.

## Пункт 43 — административная авторизация Auth.js

Auth.js 5 подключён через Credentials provider и route handler `/api/auth/[...nextauth]`. Вход
выделяет из Auth.js payload allowlist `email/password`, канонизирует email через `trim + lowercase`,
выбирает из PostgreSQL только необходимые поля и допускает исключительно пользователя с актуальной
ролью `ADMIN` и локальным `passwordHash`. Ответ при неверном email, пароле или роли одинаковый.

Credentials provider установленной версии Auth.js несовместим с database session strategy и при
такой конфигурации возвращает `UnsupportedStrategy`. Поэтому для первого административного входа
выбрана поддерживаемая JWT strategy с максимальным сроком 8 часов. JWT содержит только id и роль,
однако не является источником авторизации: общий server-only `requireAdmin()` повторно читает
пользователя из БД и проверяет текущую роль перед защищённой операцией. Это обеспечивает немедленный
запрет новых операций после снятия роли или удаления пользователя. Созданные ранее adapter-таблицы
остаются для будущих OAuth/database-сценариев и не используются для самодельного обхода Auth.js.

Пароли хешируются versioned salted `scrypt` (`N=16384`, `r=8`, `p=1`, 64-byte key, случайная
16-byte salt) средствами Node.js; сравнение выполняется constant-time, формат и параметры строго
проверяются до дорогостоящего вычисления. Создание первого администратора и запись этого hash
остаются отдельным пунктом 45; открытый пароль, hash и session token не добавляются в Git или логи.

## Пункт 44 — авторизация административных операций

Введён единый server-only wrapper `withAdminAuthorization()` для экспортируемых защищённых Route
Handlers, Server Actions и административных mutation entry points. Wrapper до вызова операции
выполняет `requireAdmin()`: получает Auth.js session, извлекает идентификатор пользователя и повторно
читает из PostgreSQL актуальные `id`, `email` и `role`. Отсутствующая session отклоняется отдельно от
пользователя без текущей роли `ADMIN`; снятие роли или удаление пользователя немедленно запрещает
следующую операцию независимо от значения роли в JWT.

Публичные схемы входа, регистрации, восстановления и изменения профиля переведены на строгие
allowlist-контракты. Неизвестные поля, включая `role` и `passwordHash`, отклоняются и не могут попасть
в будущую Prisma write-операцию через object spread или mass assignment. Роль остаётся исключительно
server-managed атрибутом; отдельное контролируемое создание первого администратора относится к
пункту 45.

На текущем этапе production CRUD Route Handlers и Server Actions ещё отсутствуют: Auth.js endpoint
является публичным transport входа, а административные операции товаров, изображений, настроек и
заказов появляются в следующих пунктах. Для них использование wrapper на каждой экспортируемой
серверной границе является обязательным, а middleware и клиентский preview-gate не считаются
авторизацией.

## Пункт 45 — создание первого администратора

Реализована отдельная server-only CLI-команда `npm run admin:create-first`. Она подключается только через
direct `DATABASE_URL_UNPOOLED`, валидирует и нормализует `FIRST_ADMIN_EMAIL`, требует пароль длиной
12–128 символов из `FIRST_ADMIN_PASSWORD` и принимает optional `FIRST_ADMIN_NAME`. Значения не передаются
CLI-аргументами, не добавляются в `.env.example`, seed, миграцию или Git и не выводятся в сообщения об
ошибках. После выполнения оператор обязан удалить одноразовые job secrets и проверить вход.

Внутри одной транзакции команда берёт фиксированный PostgreSQL transaction-level advisory lock, проверяет
отсутствие любой записи с ролью `ADMIN`, затем проверяет уникального владельца нормализованного email и
создаёт пользователя только с server-assigned ролью `ADMIN` и salted `scrypt` hash. Lock сериализует
параллельные bootstrap-запуски. Повторный запуск при существующем администраторе является no-op; совпадение
email с обычным пользователем приводит к отказу без автоматического повышения привилегий.

Команда выводит только обезличенный итог и не раскрывает connection string, email, пароль или hash.
Фактический запуск против PostgreSQL и создание реального администратора в рамках пункта не выполнялись.
Отдельная audit-модель пока отсутствует; минимальный execution audit должен обеспечиваться защищённым job
платформы до появления доменного audit log.

## Пункт 46 — административный CRUD каталога

Реализован server-only административный контракт для категорий, товаров, характеристик, групп опций,
значений опций и metadata изображений. Публичная граница каждой операции сначала повторно проверяет
текущую Auth.js session и актуальную роль `ADMIN`, а затем передаёт во внутренний сервис только ID и
входной DTO. Служебная инъекция PrismaClient остаётся внутренней и не входит в защищённый контракт.

Все create/update payload описаны строгими Zod allowlist-схемами: неизвестные поля отклоняются, цена
принимается точной decimal-строкой, даты — ISO 8601 со смещением, ID — положительной decimal-строкой,
позиции и длины полей ограничены. Валюта `BYN` назначается сервером. Prisma `data` формируется только
из прошедших проверку полей; внутренние ID, timestamps и currency нельзя назначить клиентским payload.

Существование категории, товара или группы опций проверяется в той же транзакции, где создаётся
зависимая запись. Удаление непустой категории запрещено до переноса или удаления её товаров; удаление
товара использует существующие каскадные FK для его характеристик, опций и metadata изображений.
Фактическое удаление бинарного объекта Cloudinary намеренно не выполняется: согласованный lifecycle
внешнего ресурса относится к пункту 47. Схема БД и миграции не менялись; live PostgreSQL не вызывалась.

## Пункт 47 — подписанные загрузки и lifecycle Cloudinary

Добавлен server-only адаптер установленного Cloudinary SDK. Route Handler
`POST /api/admin/uploads/signature` принимает только ID существующего товара и через защищённую
границу повторно проверяет Auth.js session и актуальную роль `ADMIN`. Сервер генерирует уникальный
`public_id` внутри `virtual-space/products/<productId>/`, подписывает короткоживущий timestamp,
allowlist raster-форматов и ограничивающую размеры transformation; API secret остаётся только на
сервере.

Результат прямой загрузки не считается доверенным metadata. Перед созданием или заменой
`ProductImage` backend получает ресурс через Cloudinary Admin API и проверяет точный `public_id`, тип
`image`, HTTPS URL, формат, размер до 10 MiB и измерения до 4096×4096. В БД записываются URL и public ID
из ответа Cloudinary, а не из клиентского payload; alt, position и идентификаторы проходят строгую
Zod allowlist-валидацию.

При неуспешной фиксации новой записи новый ресурс удаляется best-effort. При замене сначала
транзакционно переключается metadata, затем идемпотентно удаляется старый ресурс. Отдельное удаление
изображения и каскадное удаление товара сначала убирают metadata из PostgreSQL, затем очищают
Cloudinary; сбой внешней очистки возвращается как `cleanupPending`, не восстанавливая в БД ссылку на
устаревший ресурс. Гарантированная фоновая очистка потребует durable outbox/job и остаётся осознанным
остаточным ограничением до появления такой инфраструктуры. Компенсация после DB-конфликта повторно
проверяет `cloudinaryPublicId` в PostgreSQL и не удаляет ресурс, который успела зафиксировать
конкурентная операция. Prisma schema и миграции не менялись.

## Пункт 48 — защищённое управление настройками магазина

Созданы две server-only административные операции: чтение основной записи `StoreSettings` и её
полное обновление. Обе публичные границы используют `withAdminAuthorization()`, поэтому до Prisma
повторно проверяются Auth.js session и актуальная роль `ADMIN` в PostgreSQL. Отсутствующая основная
запись при чтении возвращается как отдельная доменная ошибка; автоматическое создание второй записи
или выбор клиентского ключа не допускаются.

Для update переиспользуется строгий публичный DTO настроек: неизвестные поля отклоняются, размеры
строк и массивов ограничены, контакты принимают только `https`, `mailto` и `tel`, а социальные ссылки
только `https`. Prisma write собирается явным allowlist из `name`, `description`, `contacts` и
`socials`; ключ `primary`, ID и timestamps остаются server-managed. Возвращаемое значение проходит
явный select и повторную Zod-валидацию JSON-полей. Prisma schema, миграции и данные не менялись;
подключение preview-формы `/admin/settings` остаётся пункту 49.

## Пункт 49 — подключение административного UI к Auth.js и PostgreSQL

Маршруты `/admin`, `/admin/products` и `/admin/settings` переведены с preview-gate на server-side
проверку Auth.js session и актуальной роли `ADMIN`. Credentials-форма вызывает Auth.js, выход
завершает реальную сессию, а отсутствие или недостаточная роль показывают форму входа без выполнения
административных Prisma-запросов.

Обзор рассчитывает показатели каталога через Prisma. Товары и настройки получают начальные DTO в
Server Components и изменяются через Server Actions, которые повторно используют защищённые
границы пунктов 46 и 48, валидируют allowlist DTO и инвалидируют затронутые маршруты. Preview-gate
компоненты удалены. Экран заказов и его mock transport намеренно не менялись: модели и backend
заказов относятся к следующим пунктам плана.

В UI товаров подключены чтение, создание, редактирование и удаление основных полей товара, а также
галерея: клиентская проверка файлов и preview, подписанная прямая загрузка в Cloudinary, серверная
проверка загруженного ресурса и удаление сохранённых изображений через lifecycle пункта 47. Новый
товар до успешной фиксации обязательной галереи создаётся неактивным и публикуется отдельной
защищённой мутацией только после загрузки; при замене сначала фиксируются новые ресурсы и лишь затем
удаляются выбранные старые изображения, чтобы сбой загрузки не приводил к потере прежней галереи.

## Пункт 51 — основные модели заказа

В Prisma schema добавлен `Order` с внутренним `BigInt` id и независимым уникальным `publicNumber`,
nullable-связью с `User`, обязательным снимком имени, телефона и email покупателя, optional
комментарием, валютой `BYN`, точным итогом `Decimal(12, 2)`, статусом и timezone-aware
`createdAt`/`updatedAt`. Удаление пользователя отвязывает заказ через `SetNull`, не уничтожая
контактный снимок и хозяйственную историю.

Созданы связанные валидные каркасы `OrderItem` и `OrderStatusHistory` с собственными `BigInt` id,
обязательным `orderId` и timestamps. Обе связи используют `onDelete: Restrict`, поэтому заказ с
позицией или историей нельзя удалить одним каскадом в обход принятого retention-процесса.
Обязательные снимки товарной позиции, nullable-ссылка на исходный товар, enum переходов и optional
actor намеренно не добавлены досрочно: это отдельные пункты 52–54.

До пункта 54 `Order.status` является внутренней строкой с default `NEW`; до миграции пункта 57 она
будет заменена закрытым enum `OrderStatus`. Это совместимый `schema-only` expand-шаг: существующие
таблицы и данные не изменялись, SQL-миграция, `db push`, seed и подключение к PostgreSQL не
выполнялись. Индексы путей чтения, PostgreSQL CHECK-ограничения и единая миграция остаются пунктам
55–57. Публичные DTO, backend создания заказа и UI не менялись.

## Пункт 52 — снимки товарной позиции заказа

`OrderItem` расширен обязательными `snapshotName`, `snapshotOptions`, `snapshotPrice`, `quantity` и
`lineTotal`. Название и выбранные опции сохраняются независимо от каталога, поэтому их последующее
редактирование не меняет хозяйственную историю. Выбранные опции хранятся как обязательный JSON-снимок:
конкретный строгий server-side контракт и его allowlist формируются при реализации создания заказа,
а пустая конфигурация представляется пустым массивом, а не `null`.

Цена единицы и сумма строки используют точный `Decimal(12, 2)` в той же валюте, которая зафиксирована
на родительском `Order`; количество является обязательным `Int`. Положительность количества,
неотрицательность денежных значений и согласованность итогов будут закреплены PostgreSQL `CHECK` в
пункте 55, когда все ограничения order-среза можно будет спроектировать совместно.

Изменение классифицировано как совместимое schema-only продолжение ещё не мигрированного order-среза:
существующих строк `OrderItem` в общей БД пока нет, backfill не требуется. SQL-миграция не создавалась
и БД не изменялась; она остаётся пунктом 57 после завершения моделей пунктами 53–56.

## Пункт 53 — сохранение использованного товара

`OrderItem` получил nullable `productId` и optional-связь с исходным `Product`. Nullable-ключ позволяет
сохранить обязательные снимки позиции, если ссылка когда-либо будет контролируемо отвязана, но текущая
FK-политика `onDelete: Restrict` не допускает физического удаления товара, пока на него ссылается заказ.
Обратная коллекция `Product.orderItems` делает этот инвариант явным в Prisma schema. Индекс
`OrderItem(productId)` обслуживает фактическую проверку использования и FK lookup.

Административная операция удаления теперь в одной транзакции проверяет наличие связанных позиций.
Использованный товар переводится в `isActive = false` и исчезает из публичного каталога, сохраняя саму
запись и все исторические ссылки. Только товар без позиций заказа физически удаляется по прежнему
lifecycle с последующей best-effort очисткой Cloudinary; ответ операции явно сообщает действие
`deactivated` либо `deleted`.

Изменение совместимо и относится к ещё не мигрированному schema-only order-срезу. Миграция и изменение
БД не выполнялись: единый SQL для пунктов 51–56 остаётся пункту 57. FK `Restrict` остаётся последней
защитой от гонки между проверкой и физическим удалением; при конкурентной вставке удаление отклонит БД.

## Пункт 54 — типизированная история статусов заказа

Создан закрытый Prisma enum `OrderStatus` со значениями `NEW`, `CONFIRMED`, `IN_PROGRESS`, `COMPLETED`
и `CANCELLED`. `Order.status`, а также обязательные `previousStatus` и `newStatus` каждой записи
`OrderStatusHistory` используют один тип, поэтому произвольное строковое состояние нельзя сохранить
через Prisma Client.

История хранит время изменения в timezone-aware `createdAt` и optional `changedByUserId`. Именованная
связь `changedBy` указывает на `User` и использует `onDelete: SetNull`: удаление или будущая
анонимизация аккаунта администратора не уничтожает хозяйственную историю перехода. Проверка роли
`ADMIN`, допустимого перехода и атомарное обновление заказа вместе с историей остаются обязательной
задачей server service в пункте 64; сама FK не доказывает роль пользователя.

Изменение классифицировано как совместимое schema-only продолжение ещё не мигрированного order-среза:
SQL-миграция, backfill и подключение к PostgreSQL не выполнялись. Индексы путей чтения и ограничения
целостности проектируются совместно в пунктах 55–56, а единая версионируемая миграция создаётся и
проверяется в пункте 57. Публичные DTO, backend создания заказа и административный UI не менялись.

## Пункт 55 — числовые ограничения каталога и заказа

Для числовых полей определён полный набор DB-инвариантов: остаток товара неотрицателен, количество
позиции заказа строго положительно, а `Product.price`, `Order.total`, `OrderItem.snapshotPrice` и
`OrderItem.lineTotal` неотрицательны. Все денежные поля используют точный PostgreSQL
`numeric(12, 2)` через Prisma `Decimal(12, 2)`. Дополнительно сумма позиции обязана равняться цене
единицы, умноженной на количество; согласованность общего итога с суммой позиций проверяется внутри
транзакции создания заказа, потому что межтабличный агрегат нельзя безопасно выразить строковым
`CHECK`.

Prisma 7 не поддерживает декларацию PostgreSQL `CHECK` в schema, поэтому у затронутых полей добавлены
явные комментарии-инварианты. Единая SQL-миграция пункта 57 должна материализовать их именованными
ограничениями `products_stock_nonnegative`, `products_price_nonnegative`,
`orders_total_nonnegative`, `order_items_snapshot_price_nonnegative`,
`order_items_quantity_positive`, `order_items_line_total_nonnegative` и
`order_items_line_total_matches_quantity`. Такой порядок не создаёт частичную миграцию, которая
ссылалась бы на ещё не созданные таблицы заказов, и сохраняет установленную границу пунктов 51–57.

Изменение остаётся schema-only и совместимым для текущего проекта: order-таблицы ещё не мигрированы,
существующие данные и PostgreSQL не изменялись, backfill не требуется. Server-side проверка входа и
повторный расчёт денежных значений остаются пунктам 58–60; DB-ограничения служат последней защитой, а
не заменой прикладной валидации.

## Пункт 56 — индексы чтения заказов и истории статусов

Уникальность `Order.publicNumber` уже создаёт индекс для поиска заказа по публичному номеру, поэтому
дублирующий обычный индекс не добавлен. Для пользовательской истории добавлен составной индекс
`Order(userId, createdAt DESC, id DESC)`. Административные выборки получают два пути: индекс
`Order(status, createdAt DESC, id DESC)` для фильтра по статусу с обратной хронологией и индекс
`Order(createdAt DESC, id DESC)` для общего списка и диапазона дат без status-фильтра.

История переходов использует индекс `OrderStatusHistory(orderId, createdAt DESC, id DESC)`. Во всех
хронологических индексах `id` служит стабильным tie-breaker для keyset-пагинации, если несколько
записей получили одинаковый `Timestamptz(3)`. Левые префиксы составных индексов обслуживают lookup
по `Order.userId` и `OrderStatusHistory.orderId`; остальные необходимые FK-индексы окончательно
материализованы при проверке SQL в пункте 57.

Это совместимое schema-only изменение ещё не мигрированного order-среза: таблицы не переписываются,
backfill и подключение к PostgreSQL не требуются. SQL-миграция и проверка фактических `CREATE INDEX`
остаются пункту 57; публичные DTO, серверные запросы и UI не менялись.

## Пункт 57 — миграция заказов и связанных ограничений

Создана единая версионируемая миграция `20260901120000_create_orders`. Она добавляет enum
`OrderStatus`, таблицы `Order`, `OrderItem` и `OrderStatusHistory`, связи с `User` и `Product`,
уникальность публичного номера и индексы путей чтения, определённые в пункте 56. Порядок SQL сначала
создаёт типы и таблицы, затем индексы и внешние ключи; удалений, переименований, сужения типов и
backfill нет. Дополнительные индексы `OrderItem.orderId` и `OrderStatusHistory.changedByUserId`
обслуживают загрузку состава заказа и действия внешних ключей `RESTRICT`/`SET NULL`; индекс
`OrderItem.productId` уже обслуживает retention товара.

Все семь числовых инвариантов пункта 55 материализованы именованными PostgreSQL `CHECK`. Ограничения
новых order-таблиц проверяются при каждой записи. Ограничения `Product.stock >= 0` и
`Product.price >= 0` добавляются к существующей таблице и при применении миграции валидируют все
существующие строки: наличие отрицательного legacy-значения безопасно остановит миграцию вместо его
автоматического изменения. Перед preview/production deploy оператор должен выполнить read-only
preflight-запрос на отрицательные `stock` и `price`; исправление найденных данных является отдельной
явной data migration.

Изменение классифицировано как совместимое `schema + migration`: новые таблицы не требуют backfill,
старое приложение их не использует, а текущий каталог сохраняет контракт. `ALTER TABLE "Product"`
кратковременно блокирует конкурентные изменения и сканирует существующие строки для проверки двух
ограничений, поэтому миграцию следует применять отдельным release-шагом и наблюдать её длительность.
Откат до появления order-данных возможен удалением новых таблиц, enum и двух ограничений; после
начала записи заказов rollback становится destructive, и предпочтителен forward-fix. Миграция в
этом пункте не применялась к общей или production-БД.

## Пункт 58 — серверная проверка гостевой корзины

Создан server-only контракт `validateGuestCart`, который принимает недоверенную гостевую корзину,
строго проверяет её форму и одним Prisma-запросом перечитывает уникальные товары вместе с актуальными
ценой, валютой, остатком, активностью и вариантами. Клиентский `productId` допускается только в
каноническом положительном BigInt-формате; количество строк, единиц и опций ограничено, неизвестные
поля и повторный выбор одной группы отклоняются до обращения к БД.

Сервис считает суммарное количество всех конфигураций одного товара, требует ровно один существующий
вариант для каждой актуальной группы и не принимает отсутствующий или неактивный товар. Цена из
localStorage используется только для обнаружения изменения: валидная позиция и снимки для будущего
заказа формируются из серверных имени, подписей опций и точного `Decimal(12, 2)`, преобразованного в
канонический money DTO. Результат разделяет проверенные позиции и типизированные причины
`PRODUCT_UNAVAILABLE`, `INSUFFICIENT_STOCK`, `INVALID_CONFIGURATION`, `PRICE_CHANGED`; последняя
содержит текущую цену для контролируемого конфликта пункта 59.

Проверка остаётся read-only и не резервирует остаток: конкурентно безопасное повторное чтение,
уменьшение остатка и создание заказа в одной транзакции относятся к пункту 60. Схема Prisma,
миграции и данные не менялись; отдельный HTTP transport не добавлялся, поскольку публичный конфликт
и повторное подтверждение цены реализуются следующим пунктом.

## Пункт 59 — контролируемый конфликт цены

Добавлена checkout-preflight граница с закрытым результатом `READY | CONFLICT`. `READY` содержит
только полностью проверенные server-owned позиции; при любой причине конфликта частично валидные
позиции не возвращаются как готовые к созданию заказа. Это не позволяет вызывающему коду случайно
смешать доверенные данные PostgreSQL с остатком гостевой корзины из localStorage.

Публичный `POST /api/orders/preflight` возвращает HTTP `409` и типизированные issues. Для
`PRICE_CHANGED` ответ включает актуальный канонический money DTO. Новая цена считается явно
подтверждённой только после повторного запроса, в котором клиент передал её как новое
`observedPrice`; повтор старого значения снова даёт `409`. Невалидный JSON/DTO отображается в
безопасный `400`, неожиданная серверная ошибка — в общий `500` без внутренних деталей.

Контракт остаётся read-only: он не резервирует остаток, не создаёт заказ и не закрывает гонку между
preflight и записью. Пункт 60 обязан повторить проверку и изменить остатки внутри одной транзакции,
используя только server-owned позиции. Prisma schema, миграции, данные и пользовательский UI в этом
пункте не менялись.

## Пункт 60 — атомарное создание гостевого заказа

Добавлен публичный `POST /api/orders` и server-only сервис создания заказа. Контактные данные и
корзина проходят строгий allowlist, после чего сервис внутри одной serializable Prisma-транзакции
повторно читает товары и варианты. Клиентские цена, итог, название и подписи опций не используются
для записи: `Decimal`-итог и каждый снимок `OrderItem` строятся только из актуального результата
PostgreSQL.

Суммарное количество всех конфигураций одного товара уменьшается одним условным `updateMany` с
`isActive = true` и `stock >= quantity`. Проигранная гонка создаёт контролируемый конфликт и
откатывает все предыдущие уменьшения; serialization/deadlock-конфликты `P2034` ограниченно
повторяются. После успешных уменьшений одним nested create сохраняются `Order`, все `OrderItem` и
начальная история `NEW -> NEW`. Такая пара статусов явно фиксирует установку начального состояния,
поскольку schema требует оба значения, а состояния до создания заказа не существует.

Схема и миграция не менялись, подключение к PostgreSQL не выполнялось. UI checkout остаётся на
preview-transport до пункта 65; полный набор transaction/concurrency проверок плана остаётся пунктам
66–67. Отдельное persistent idempotency-хранилище и инфраструктурный rate limit не добавлялись: они
требуют отдельного контракта/схемы и остаются release-hardening перед публичным запуском.

## Пункт 61 — закрытая матрица переходов статуса заказа

Добавлен server-only доменный инвариант на основе Prisma `OrderStatus`. Разрешены ровно шесть
переходов: `NEW -> CONFIRMED`, `NEW -> CANCELLED`, `CONFIRMED -> IN_PROGRESS`,
`CONFIRMED -> CANCELLED`, `IN_PROGRESS -> COMPLETED` и `IN_PROGRESS -> CANCELLED`. Все остальные
пары, включая повтор текущего статуса и выход из `COMPLETED`/`CANCELLED`, отклоняются
контролируемой доменной ошибкой.

Проверка не выполняет мутаций и предназначена для вызова до первой записи, поэтому отклонённый
переход сам по себе не создаёт частичного состояния. Prisma schema и данные не изменяются.
Транзакционное обновление `Order.status` вместе с `OrderStatusHistory`, повторная проверка текущего
статуса и административная авторизация остаются границей пункта 63.

## Пункт 62 — безопасное чтение заказов

Добавлен публичный `POST /api/orders/lookup`, который не помещает email покупателя в URL. Для
авторизованного пользователя заказ выбирается по публичному номеру и `userId` из серверной Auth.js
session. Для гостя обязательна пара публичного номера и нормализованного email, а выборка требует
`userId IS NULL`; отсутствие заказа и несовпадение владельца имеют одинаковый безопасный `404`.
Контракт возвращает только статус, даты, точный money DTO и снимки позиций без контактных данных,
Prisma-моделей и внутреннего идентификатора заказа.

Защищённый `GET /api/admin/orders` повторно проверяет Auth.js session и актуальную роль `ADMIN` в
PostgreSQL. Выборка ограничена максимум 100 строками, использует детерминированную обратную
хронологию и cursor pagination по уникальному `publicNumber`. Только административный DTO включает
необходимые контактные снимки; обе выборки используют явные Prisma `select`.

Схема, миграции и данные не менялись. Интеграция экранов остаётся пункту 65. Публичный номер содержит
48 случайных бит, а гостевой lookup дополнительно требует email; платформенный rate limit от
автоматизированного перебора остаётся общему hardening пункта 78.

## Пункт 63 — административное изменение статуса заказа

Добавлен защищённый `PATCH /api/admin/orders/[orderNumber]/status`. Экспортируемая server-only
граница перед каждой мутацией заново получает Auth.js session и сверяет актуальную роль `ADMIN` в
PostgreSQL; идентификатор администратора берётся только из этой проверки и сохраняется в истории.

Внутри одной Prisma-транзакции заказ повторно читается по публичному номеру, текущий статус
проверяется закрытой матрицей пункта 61, после чего `Order.status` обновляется условно по паре
`id + previousStatus`. Если конкурентный запрос уже изменил строку, условное обновление не затрагивает
заказ, история не создаётся, а transport возвращает `409`. После успешного обновления в той же
транзакции создаётся `OrderStatusHistory` с предыдущим и новым статусом; любая ошибка откатывает обе
записи.

Вход ограничен публичным номером заказа и Prisma enum `OrderStatus`; неизвестные поля отклоняются.
Ответ содержит только публичный номер, новый статус и время обновления. Schema, миграции и данные не
менялись. UI `/admin/orders` остаётся на preview transport до пункта 65.

## Пункт 64 — best-effort уведомление Telegram после commit

Добавлен server-only адаптер Telegram Bot API на встроенном `fetch`, без новой зависимости. Он
читает и валидирует `TELEGRAM_BOT_TOKEN` и `TELEGRAM_ADMIN_CHAT_ID` только на сервере, использует
HTTPS endpoint Telegram, JSON POST без кеширования и timeout 5 секунд. В уведомление входят только
публичный номер, серверный итог и статус заказа; имя, телефон, email, комментарий и состав заказа не
передаются.

Сервис создания заказа вызывает адаптер только после успешного разрешения serializable Prisma-
транзакции, то есть после commit. Если транзакция отклонена, внешний вызов не выполняется. Ошибка
конфигурации, сети, timeout, HTTP или ответа Telegram перехватывается отдельно: заказ возвращается
клиенту как успешно созданный, а в server log попадает только общее сообщение без токена,
chat ID и персональных данных.

Гарантированная доставка, повторные попытки и outbox не добавлялись: они требуют отдельного
persistent состояния и не входят в MVP-решение. Schema, миграции и данные не менялись; для работы
интеграции окружение deployment должно предоставить обе Telegram-переменные.

## Пункт 65 — интеграция checkout и административных заказов

Checkout переведён с генерации preview-номера на публичный `POST /api/orders`. Клиент отправляет
только контактный allowlist и сохранённые идентификаторы корзины с наблюдаемой ценой; сервер
повторно проверяет PostgreSQL и возвращает номер, статус `NEW` и точный money DTO. Корзина очищается
только после HTTP `201`. При `409` она сохраняется, а `PRICE_CHANGED` переносится в `observedPrice`
только после явного действия пользователя, поэтому повторная отправка служит подтверждением новой
стоимости.

Маршрут `/admin/orders` больше не использует preview-session и mock-данные: первая ограниченная
страница загружается на сервере после проверки Auth.js и актуальной роли `ADMIN`. Повторная загрузка
использует защищённый GET, а смена статуса — защищённый PATCH. Клиентский mapper валидирует
allowlisted DTO и переводит серверные enum-статусы в существующую UI-модель; бизнес-инвариант и
история остаются внутри server-only транзакции.

Prisma schema, миграции и данные не менялись. Persistent idempotency заказа и платформенный rate
limit не добавлялись и остаются release-hardening; потеря успешного ответа при повторной ручной
отправке теоретически может создать второй заказ.

## Пункт 66 — транзакционные и авторизационные проверки заказов

Серверный контур заказа проверен unit/integration-тестами без подключения к общей или production-БД.
Новый транзакционный harness фиксирует состояние до callback и восстанавливает его при исключении:
ошибка создания `Order` после уменьшения остатка не оставляет списанный товар, а ошибка создания
`OrderStatusHistory` после обновления `Order.status` не оставляет новый статус без истории. Так
подтверждена зависимость полного rollback от единой Prisma-транзакции обоих сервисов.

Отдельная проверка фиксирует серверный перерасчёт суммы и полный неизменяемый снимок позиции:
`productId`, название, выбранные option/group labels, точная цена, количество и итог строки. Вместе
с существующими тестами подтверждены начальная запись истории `NEW -> NEW`, запись администратора при
смене статуса, повторная проверка актуальной роли `ADMIN`, отсутствие вызова сервиса при отказе в
доступе и отклонение переходов вне закрытой матрицы до записи истории.

Production-код, Prisma schema, миграции и данные не менялись. Проверка конкурентного оформления
последней единицы и гарантии неотрицательного остатка остаётся отдельной границей пункта 67.

## Пункт 67 — конкурентное оформление последней единицы

Добавлен отдельный конкурентный transaction harness для сервиса создания заказа. Две операции
синхронизируются так, чтобы обе прочитали исходный остаток `1` до попытки списания. Условный
`updateMany` по `id + isActive + stock >= quantity` атомарно пропускает только одну операцию:
победитель создаёт единственный заказ и оставляет остаток `0`, проигравший получает контролируемый
`INSUFFICIENT_STOCK` и не создаёт заказ.

Тест подтверждает прикладной инвариант без подключения к общей или production-БД. На уровне
PostgreSQL его дополнительно страхует существующий `CHECK (stock >= 0)`, а production runner
использует `Serializable` и ограниченный retry конфликтов `P2034`. Read-only security review
проверил серверный пересчёт, атомарность списания, transaction boundary, ограничение количества и
безопасный конфликт; подтверждённых findings нет. Остаточный release-риск — отсутствие отдельного
теста против живого PostgreSQL с реальными блокировками и уровнем изоляции.

Production-код, Prisma schema, миграции и данные не менялись. Изменение классифицировано как
test-only проверка уже реализованного инварианта целостности; миграционный путь и rollback не
требуются.

## Пункт 68 — фундамент пользовательских сценариев Auth.js

Существующие совместимые с Auth.js модели `User`, `Account`, `Session` и `VerificationToken`, ранее
добавленные для административного контура, переиспользуются для покупателей. В `User` добавлена
server-managed `credentialsVersion` с безопасным default `0`: при смене или восстановлении пароля
backend пункта 72 обязан атомарно увеличить её и отклонять JWT со старой версией на защищённых
пользовательских границах.

Для credentials-восстановления добавлена отдельная `PasswordResetToken`. В PostgreSQL хранится только
уникальный hash случайного токена, ссылка на пользователя, timezone-aware срок действия и время
создания. Индексы обслуживают lookup по hash, отзыв токенов пользователя и очистку истёкших записей;
удаление пользователя каскадно удаляет только его reset-токены. Отправка письма, одинаковый внешний
ответ для существующего и неизвестного email, генерация секрета и атомарное потребление токена
остаются server-контрактом пункта 72.

Миграция профиля согласована без переноса демонстрационной записи: зарегистрированный пользователь
получает профиль из `User.name`, канонического `User.email` и optional `User.phone`. Гостевые заказы
не присваиваются новому аккаунту только по совпадению email, поскольку это создаёт takeover истории;
их `userId` остаётся `NULL`, а контактные снимки неизменяемы. Будущий claim возможен только после
доказательства владения адресом отдельной транзакционной операцией.

Изменение классифицировано как совместимое `schema + migration`: новая таблица additive, а
`credentialsVersion` получает значение `0` для существующих пользователей. Удалений, переименований,
сужения типов и backfill персональных данных нет. Rollback до использования функции может удалить
новую таблицу и колонку; после выпуска reset-сценария предпочтителен forward-fix, чтобы не обрывать
активные восстановления и не возвращать валидность старым JWT. Миграция не применялась к живой БД,
`db push`, seed и production-запросы не выполнялись.

## Пункт 69 — модели активной пользовательской корзины

Добавлены Prisma-модели `Cart` и `CartItem`. `Cart` представляет только текущую активную корзину:
история корзин не хранится, а unique-ограничение `userId` не допускает более одной записи на
пользователя. Позиция ссылается на корзину и товар, хранит количество, наблюдаемую точную цену,
выбранные опции в JSON и отдельный `optionsKey`.

`optionsKey` является server-managed каноническим представлением проверенных выбранных опций:
backend обязан отсортировать пары стабильных идентификаторов группы и значения и сериализовать их
одинаковым способом независимо от порядка клиентского массива. Unique-ограничение
`(cartId, productId, optionsKey)` исключает две позиции одной конфигурации; доверять присланному
клиентом ключу нельзя. Пустой набор опций получает единое каноническое значение.

Изменение классифицировано как совместимое schema-only: новые обязательные таблицы пока существуют
только в Prisma schema и не влияют на текущие строки. Явная политика `onDelete`, дополнительные
индексы, PostgreSQL `CHECK` для количества/цены и SQL-миграция намеренно остаются пунктам 70–71;
миграция, `db push`, seed и обращения к живой БД не выполнялись. До реализации backend пункта 73
модели не являются публичным API и не заменяют гостевую Zustand-корзину.

## Пункт 70 — удаление аккаунта, корзины и связи с заказами

Prisma schema теперь выражает принятую в пункте 25 политику явно:

- `Cart.user` и `CartItem.cart` используют `onDelete: Cascade`, поэтому удаление identity или
  активной корзины не оставляет пользовательские позиции;
- `CartItem.product` использует `onDelete: Restrict`: активная позиция должна быть удалена или
  обработана доменной операцией до удаления товара;
- `Order.user` и `OrderStatusHistory.changedBy` используют `onDelete: SetNull`, поэтому удаление
  покупателя или администратора не разрушает заказ и историю переходов;
- `OrderItem.order` и `OrderStatusHistory.order` остаются `Restrict`: история заказа не удаляется
  неявным каскадом;
- `User.deletedAt` фиксирует необратимую анонимизацию identity, а optional `Order.retentionUntil` —
  отдельный срок хранения заказа. Контактные поля `Order.customerName`, `phone`, `email` и `comment`
  остаются снимком покупателя и не зависят от последующих изменений или анонимизации `User`.

Изменение классифицировано как совместимое schema-only: добавлены nullable-поля и уточнены referential
actions для ещё не мигрированных таблиц корзины; существующая связь заказа уже использовала
`SET NULL`. SQL-миграция, индексы и PostgreSQL `CHECK` остаются пункту 71. Подключение к живой БД,
`db push`, seed и backfill не выполнялись. До появления server-only операции удаления аккаунта
`deletedAt` является инвариантом хранения, но не самостоятельной реализацией отзыва доступа.

## Пункт 71 — индексы и миграция пользовательской корзины

Версионированная миграция материализует согласованные в пунктах 69–70 таблицы `Cart` и `CartItem`,
nullable-поля `User.deletedAt` и `Order.retentionUntil`, referential actions и DB-level инварианты
`quantity > 0` и `observedPrice >= 0`. Изменение классифицировано как совместимое `schema +
migration`: новые таблицы и nullable-поля additive, существующие строки и публичные контракты не
переписываются. Применение миграции к живой БД, `db push`, seed и backfill не выполнялись.

Поиск единственной активной корзины пользователя покрывает unique-индекс `Cart.userId`; отдельный
дублирующий индекс с тем же префиксом не создаётся. Unique-индекс
`CartItem(cartId, productId, optionsKey)` обеспечивает идентичность канонической конфигурации, а
отдельный `(cartId, createdAt, id)` — стабильную выборку позиций без сортировки всего набора.
`CartItem.productId` индексирован для FK-проверок и будущих product-scoped операций. Существующий
`Order(userId, createdAt DESC, id DESC)` уже соответствует пути истории заказов пользователя и
повторно не создаётся.

SQL проверен на отсутствие `DROP`, сужения типов, table rewrite и обязательного backfill. Создание
обычных индексов берёт блокировку на время построения, поэтому миграцию нужно сначала прогнать на
preview или восстановленной непроизводственной копии; для текущих новых пустых таблиц риск
блокировки минимален. Rollback до использования серверной корзины может удалить новые таблицы и
nullable-поля; после записи данных предпочтителен forward-fix, чтобы не потерять корзины и
retention-метаданные. Проверка `EXPLAIN` на репрезентативных данных остаётся пункту 83.

## Пункт 72 — пользовательская авторизация и профиль

Credentials provider теперь допускает активные учётные записи `USER` и `ADMIN`, сохраняет в JWT
server-managed `credentialsVersion` и использует `/login` как общую страницу входа. Защищённые
границы перечитывают аккаунт из PostgreSQL и отклоняют удалённого пользователя либо JWT, версия
которого перестала совпадать после смены пароля. Административный wrapper сохраняет отдельную
проверку актуальной роли `ADMIN`.

Регистрация принимает строгий `name/email/password`, канонизирует email, хеширует пароль через
существующий versioned `scrypt` и всегда создаёт роль `USER`. Конфликт unique email отображается в
стабильный transport-код; Prisma-модель и password hash наружу не возвращаются.

Восстановление создаёт 256-битный одноразовый секрет со сроком 30 минут, удаляет прежние токены
пользователя и хранит только SHA-256 hash. Неизвестный, удалённый и не-credentials аккаунт получают
одинаковый внутренний результат `null`, чтобы публичный transport доставки мог отвечать одинаково.
Подтверждение повторно проверяет токен, атомарно потребляет его, меняет password hash, увеличивает
`credentialsVersion` и отзывает остальные reset-токены. Сырой секрет остаётся server-only handoff
для выбранного email-адаптера; провайдер доставки не добавлен без продуктового решения и не
имитируется логированием или публичным возвратом токена.

Профиль читает identity только из Auth.js session и разрешает менять исключительно `name`,
канонический `email` и `phone`; user id, role и credentials-поля отсутствуют во входе. UI `/login` и
`/profile` намеренно остаётся на preview-transport до интеграционного пункта 76. Prisma schema и
миграции в пункте 72 не менялись.

## Пункт 73 — API серверной корзины

Persistent-корзина доступна только через актуальную Auth.js identity: сервис получает `userId` из
`requireUser()`, а чтение и каждая мутация ограничены relation-фильтром владельца. Транспортом для
собственного UI выбраны Server Actions; бизнес-проверки и Prisma-запросы остаются в server-only
сервисе модуля `cart`.

Публичная идентичность позиции состоит из `productId` и выбранных стабильных ключей опций.
`optionsKey` клиентом не передаётся: сервер строит однозначную length-prefixed строку из
отсортированных пар. Изменение количества принимает диапазон `1..99`, повторно проверяет активность
товара, полный набор опций и суммарный остаток товара по остальным конфигурациям корзины, после чего
записывает текущую Prisma `Decimal`-цену. Удаление использует тот же канонический ключ и
ownership-предикат. Отсутствующая или чужая позиция получает одинаковый `ITEM_NOT_FOUND`.

Чтение является операцией ревалидации и возвращает каждой позиции один из статусов `AVAILABLE`,
`PRICE_CHANGED`, `PRODUCT_UNAVAILABLE`, `INVALID_CONFIGURATION` или `INSUFFICIENT_STOCK`, а цены —
только через денежный DTO `{ amount, currency }`. Prisma-модели, cart/item id, timestamps и
внутренние ошибки не пересекают transport. Создание позиций и объединение гостевой корзины
намеренно остаются пункту 74; подключение UI — пункту 76. Схема и миграции не изменялись.

## Пункт 74 — безопасное объединение корзин

Добавлен отдельный server-only merge-контракт и Server Action для собственного UI. Вход содержит
только до 100 позиций с каноническим положительным `productId`, количеством `1..99` и выбранными
stable key опций; клиентские `observedPrice`, `optionsKey`, user/cart id и неизвестные поля
отклоняются строгой Zod-схемой. Identity пользователя всегда берётся через `requireUser()`.

Одинаковые гостевые конфигурации независимо от порядка опций получают один server-managed
length-prefixed `optionsKey`; их количество суммируется с уже сохранённой позицией. Товар, полный
набор опций, активность, общий остаток по всем конфигурациям и итоговый лимит позиции проверяются по
PostgreSQL. В `CartItem.observedPrice` записывается только актуальная Prisma `Decimal`-цена товара.

Merge имеет all-or-nothing семантику в `Serializable`-транзакции: любой конфликт возвращает
типизированные issues и не оставляет частично перенесённых позиций. Ограниченный retry применяется
для сериализационного `P2034` и unique-конфликта `P2002`, возможного при одновременном создании
единственной корзины пользователя. Пустой гостевой снимок является безопасным no-op и возвращает
актуальную серверную корзину. Prisma schema, миграции и данные не менялись; очистка Zustand после
успешного merge и переключение UI относятся к пункту 76.

## Пункт 75 — доступ только к собственным пользовательским ресурсам

Профиль и persistent-корзина сохраняют ранее реализованную server-owned identity: каждая публичная
Server Action делегирует в server-only сервис, который вызывает `requireUser()` и использует только
полученный из актуальной Auth.js-сессии `User.id`. Клиент не может передать владельца профиля,
корзины или позиции; Prisma read/write корзины ограничены relation-фильтром `cart.userId`.

Для истории заказов добавлена отдельная bounded cursor-выборка. Вход принимает только строгие
`cursor` и `limit` (максимум 50), после валидации перечитывает актуального пользователя и выполняет
`Order.findMany` с обязательным `where.userId = principal.id`. Сортировка
`createdAt DESC, id DESC` соответствует существующему индексу, а наружу возвращаются только
публичный номер, статус, даты, денежный DTO и снимки позиций — без контактов, внутренних order/user id
и Prisma-моделей. Server Action отображает отсутствие сессии, невалидный ввод и неожиданный сбой в
стабильные коды без внутренних деталей.

Схема и миграции не менялись: используется индекс `Order(userId, createdAt DESC, id DESC)` из пункта 71. UI `/profile` остаётся на preview-данных до пункта 76. Доступ к чужой истории проверен unit-тестом
структуры Prisma-запроса и запретом входного `userId`; подключение к живой PostgreSQL не выполнялось.

## Пункт 76 — интеграция пользовательского UI

`/login`, `/profile` и корзина переведены с preview/sessionStorage transport на Auth.js и защищённые
Server Actions. После входа недоверенный гостевой снимок проходит строгую серверную валидацию и
атомарный merge; клиент затем заменяет локальное состояние allowlist DTO из PostgreSQL. Изменения
количества и удаление сериализуются на клиенте, но повторно проверяются сервером с ownership по
актуальной Auth.js session. Цена, наличие и конфигурация из localStorage не являются источником
истины. Prisma schema и миграции не менялись.

Серверный cart DTO дополнен публичными presentation-полями товара (`slug`, основное изображение и
alt), чтобы persistent-позиции можно было отрисовать после нового сеанса без mock-каталога. Эти поля
не участвуют в расчёте цены или доступности. Raw reset token не возвращается в UI; до выбора email-
провайдера восстановление честно сообщает, что доставка письма не настроена.

## Пункт 78 — единая защита серверных HTTP-границ

Route Handlers заказов и административных мутаций переведены на общий server-only boundary. Он
принимает только безопасный ограниченный `x-request-id` либо создаёт UUID, добавляет идентификатор в
response header, единообразно отображает ошибки Zod/JSON в `400`, возвращает прежние безопасные
endpoint-specific сообщения для `500` и журналирует неожиданный сбой только как allowlist полей
`requestId`, operation, method, path и error type. Текст исключения, stack, body, query, headers,
контакты, credentials и Prisma-данные в лог не попадают.

По фактической модели угроз локальный fixed-window limiter включён для создания и preflight заказа,
поиска заказа, выдачи upload-signature, смены статуса и credential-login. Ключ HTTP-запроса строится
из адреса, переданного доверенным reverse proxy; email для credential-login сначала преобразуется в
SHA-256 и в памяти в открытом виде не хранится. Превышение HTTP-лимита возвращает `429` и
`Retry-After`; login сохраняет одинаковый отказ credentials, чтобы не создавать новый канал
перебора аккаунтов.

Limiter намеренно не заявлен распределённым: его память ограничена процессом и сбрасывается при
рестарте. Перед горизонтальным production-развёртыванием требуется платформенный/distributed rate
limiting и конфигурация доверенного proxy, иначе `x-forwarded-for` нельзя считать надёжным
источником. Новые зависимости, переменные окружения, Prisma schema и миграции не добавлялись.

## Пункт 79 — жизненный цикл версионированных миграций

Подготовка изменения схемы формализована отдельным протоколом `docs/database-migrations.md`.
Физическое изменение PostgreSQL всегда поставляется вместе с использующим его кодом и новой
timestamped-директорией `prisma/migrations`; уже зафиксированные SQL и `migration_lock.toml`
считаются неизменяемой историей, а исправления выполняются следующей forward-fix миграцией.

Локальная команда `npm run prisma:check-migrations` сравнивает feature-ветку с `origin/main` или
`main`, проверяет структуру migration tree, запрещает изменение/удаление существующей истории и
требует новый `migration.sql` при изменении `prisma/schema.prisma`. В CI база сравнения должна
задаваться явно через `MIGRATION_BASE_REF`, чтобы shallow clone не ослаблял проверку.

Несовместимые изменения разбиваются на отдельные релизы `expand -> migrate -> contract`: сначала
добавляется обратно совместимая структура, затем выполняется ограниченный backfill и переключение
потребителей, и только после проверки отсутствия старых потребителей удаляется прежняя структура.
`prisma db push` не является миграционным путём для shared local, preview или production.

Пункт не изменяет Prisma schema, SQL-историю или данные и не запускает миграции. Автоматизация
отдельного `prisma migrate deploy` release-job и проверка на preview относятся к пункту 80;
production pipeline и platform concurrency — к пункту 82.

## Пункт 80 — отдельный release-шаг миграций

Добавлена release-only команда `npm run prisma:migrate:deploy`, которая применяет версионированную
историю через Prisma CLI и direct `DATABASE_URL_UNPOOLED`. Она принимает только целевую среду и
полный SHA immutable artifact, требует чистый Git checkout и перед deploy повторно выполняет
`prisma:check-migrations`. Ни `build`, ни `start`, ни runtime entry points команду не вызывают, поэтому
serverless cold start не может запустить миграцию.

Preview сначала получает тот же commit, который планируется выпустить. Production-команда требует
`--preview-verified-sha`, полностью совпадающий с `--artifact-sha`; изменение commit после проверки
аннулирует evidence и требует нового preview-прогона. Изолированная preview Neon-ветка или
восстановленная непроизводственная копия не должна содержать немаскированные production-данные.

Пункт классифицирован как release/configuration change: Prisma schema, SQL-история и данные не
менялись, live-БД не подключалась и `migrate deploy` фактически не выполнялся. Guard-условия можно
проверить через `--dry-run`; production pipeline, protected environment и concurrency lock остаются
пункту 82.

## Пункт 81 — production environment и восстановление

Добавлен отдельный preflight `npm run production:validate-env`. Он проверяет наличие непустых
`DATABASE_URL`, `DATABASE_URL_UNPOOLED`, `AUTH_SECRET`, `AUTH_URL`, Cloudinary и Telegram переменных,
но при успехе или отказе не выводит ни одного значения. `OPENAI_API_KEY` пока не включён в обязательный
набор, поскольку AI-функция не реализована. Подключение команды к protected deployment pipeline
остаётся пункту 82.

Prisma seed остаётся явной `npm run prisma:seed` local/preview-командой. Её JavaScript wrapper и
сам TypeScript seed безусловно отказывают при `NODE_ENV=production` до создания Prisma Client. Seed
не вызывается из build, start или migration job; production-данные он не изменяет.

В `docs/production-operations.md` зафиксированы обязательные managed backup/PITR и restore drill,
восстановление только в новый изолированный target, защита backup и credentials, а также приоритет
новой версионированной forward-fix миграции над изменением применённого SQL или destructive rollback.
Конкретные provider-команды, retention, RPO/RTO, protected environments и автоматизация фиксируются
при provisioning в пункте 82. Live-БД, backup, restore и seed не запускались.

## Пункт 82 — защищённый production release

Production release реализован как ручной GitHub Actions workflow для полного lowercase Git SHA из
`main`. Один и тот же immutable artifact сначала проходит проверки, миграцию и Vercel deployment в
изолированном GitHub Environment `release-preview`; только затем job входит в защищённый Environment
`production`, ожидает approval, повторно подтверждает SHA, применяет те же preview-verified миграции
и выполняет production build/deploy.

Runtime и migration configuration берутся из соответствующего Vercel Environment. Prisma runtime
использует pooled `DATABASE_URL`, а release CLI — direct `DATABASE_URL_UNPOOLED`; env-файл хранится
только на ephemeral GitHub runner и исключён из Git. GitHub хранит только scoped Vercel deployment
credentials. Secrets выдаются отдельным CLI-шагам и не доступны `npm ci`, lint, typecheck или Jest.

Production migration и rollout сериализованы concurrency-группой `production-release` без отмены
уже выполняющегося job. GitHub Actions закреплены по полным commit SHA, Vercel CLI — по точной версии
и предварительно кешируется без secrets. Автоматический Vercel production deployment из Git должен
быть отключён, иначе он сможет обойти порядок `migration -> rollout`.

Внешнее provisioning не выполнялось: владелец инфраструктуры создаёт Vercel/Neon resources,
раздельные preview/production credentials, required reviewers, branch restrictions, backup/PITR и
restore drill по `docs/production-operations.md`. Prisma schema, SQL migration history и production
данные пунктом не изменялись; workflow локально не запускал Vercel deploy или live migration.

## Пункт 83 — проверка планов основных запросов

Добавлена явная команда `npm run prisma:verify-query-plans` для проверки основных PostgreSQL-путей
каталога, корзины и заказов. Она создаёт в одной транзакции 2 000 товаров, 2 000 заказов, целевую и
фоновые корзины, обновляет статистику и выполняет `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)` для
общего и категорийного каталога, пользовательской корзины, собственной и административной истории
заказов, а также поиска заказа по публичному номеру. Проверка завершается ошибкой, если план не
содержит ожидаемый индекс, и безусловно откатывает транзакцию в `finally`.

Команда запрещена при `NODE_ENV=production`, принимает только `QUERY_PLAN_ENV=local|test|preview` и
дополнительно требует осознанное подтверждение rollback-only записей через
`QUERY_PLAN_ALLOW_WRITES=1`. Подключение выполняется только через direct
`DATABASE_URL_UNPOOLED`; строка подключения, параметры и строки данных не выводятся. Запуск:

```powershell
$env:QUERY_PLAN_ENV = "preview"
$env:QUERY_PLAN_ALLOW_WRITES = "1"
npm run prisma:verify-query-plans
```

Prisma schema и история SQL-миграций не изменены: проверяемые индексы уже соответствуют фактическим
путям запросов. В текущем рабочем окружении отсутствовали непроизводственная PostgreSQL и
`DATABASE_URL_UNPOOLED`, поэтому фактическое evidence `EXPLAIN` должно быть получено отдельным
прогоном команды на local/preview target; статическая проверка не считается его заменой.

## Пункт 84 — серверные regression-тесты и финальный security-аудит

К уже существующему покрытию ключевых заказных сценариев добавлены отдельные regression-тесты
двух чувствительных границ. Cloudinary adapter теперь проверяется на фиксированные форматы,
ограничение размера и разрешения, HTTPS resource DTO и image-only параметры удаления. Password reset
проверяется на удаление просроченного токена до вычисления нового password hash и на атомарный отказ,
если одноразовый токен уже потреблён конкурентным запросом. Существующие тесты не изменялись.

Новые E2E не создавались: пункт допускает их только после отдельного явного разрешения на конкретный
критический сценарий. Полный Jest-прогон перед изменением содержал 94 suites и 290 тестов; новые два
набора добавляют 7 тестов. Live PostgreSQL, Cloudinary и production-инфраструктура не использовались,
внешние вызовы заменены контролируемыми adapter mocks.

Финальный read-only security-review подтвердил один `Medium` finding: публичные Server Actions
регистрации и восстановления пароля не имеют общего rate limiting. Анонимный клиент может повторно
запускать password hashing и записи PostgreSQL, создавая вычислительную и DB-нагрузку; recovery для
существующего адреса также многократно заменяет активный токен. Минимальное последующее исправление —
серверный distributed limiter по доверенному IP и нормализованному hash email с отдельными лимитами
для регистрации, запроса и применения reset-токена. Оно не включено в read-only аудит пункта 84.

Архитектура, API-контракты, Prisma schema, миграции и пользовательские маршруты не изменялись.

## Пункт 85 — финальная приёмка первого DB-релиза

Финальная приёмка выполнена на Node.js/npm-окружении проекта: Prisma schema валидна, Prisma Client
7.9.1 воспроизводимо генерируется, полный Jest-набор содержит 96 suites и 297 успешно пройденных
тестов, ESLint и TypeScript проходят, production build Next.js 16.3.0 завершается успешно.

Для статической Prisma-проверки и production build использовались только локальные placeholder-
значения обязательных переменных в окружении процесса. Они не записывались в файлы и не применялись
для подключения к внешним сервисам. Реальные `.env`, credentials и production-данные не читались.

Архитектура и пользовательские сценарии этим пунктом не изменены, поэтому `docs/architecture.md` и
`docs/ProductTour.md` не обновлялись. Новые E2E не создавались и существующие E2E не запускались:
пункт не меняет UI или сквозной сценарий, а обязательный regression-набор покрыт полным Jest-прогоном.

Приёмка не заменяет оставшееся внешнее evidence: проверку `EXPLAIN` пункта 83 нужно выполнить на
репрезентативной local/preview PostgreSQL, а production provisioning, migration/deployment и
backup/restore drill — в целевой инфраструктуре. Также остаётся подтверждённый пунктом 84 `Medium`
security finding об отсутствии distributed rate limiting публичных auth Server Actions.

## Исправление первого production bootstrap после пункта 85

Первый фактический production rollout подтвердил, что versioned schema migrations применяются, но
намеренно не запускаемый production seed оставляет новую базу без обязательной записи
`StoreSettings.primary`. Для устранения этого разрыва добавлена отдельная data-only migration,
которая вставляет публичный профиль магазина через `ON CONFLICT ("key") DO NOTHING`.

Решение сохраняет границу безопасности seed: общий каталог и тестовые fixture-данные автоматически
в production не загружаются, а уже созданные администратором настройки не перезаписываются. Prisma
schema не меняется; rollback для единственной bootstrap-записи намеренно не автоматизируется, чтобы
не удалить настройки, которые оператор мог изменить после release.

Auth.js дополнительно получает `AUTH_SECRET` явным server-only параметром конфигурации. Значение
секрета не хранится в Git, не выводится в логи и остаётся обязательной переменной Vercel Production.

## Загрузка versioned каталога после первого production release

Чтобы production-каталог соответствовал демонстрационному содержимому GitHub без снятия запрета на
общий production seed, добавлен отдельный catalog bootstrap. Он запускается release workflow после
успешных миграций сначала в Preview, затем в Production, и читает тот же `catalogSeed`, который
формируется из versioned `src/modules/catalog/mock-data.ts`.

Операция совместима с существующими данными: категории создаются только при отсутствии, а товар со
всеми изображениями, характеристиками, группами и вариантами создаётся атомарно только при отсутствии
его уникального `slug`. Существующие товары и категории не обновляются и не удаляются. Весь bootstrap
выполняется в транзакции; конфликт глобального ключа дочерней записи приводит к полному откату, а не
частично загруженному каталогу.
